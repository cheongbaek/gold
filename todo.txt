═══════════════════════════════════════════════════════════════════════════════
 white1 실차 시험 목록                                          [2026-08-14 기준]
═══════════════════════════════════════════════════════════════════════════════
 끝난 항목은 [ ] → [x] 로 바꾼다. 결과 숫자는 그 자리에 바로 적어 둘 것 —
 나중에 로그를 다시 뒤지는 것보다 그때 적어 두는 편이 언제나 빠르다.

───────────────────────────────────────────────────────────────────────────────
 0. 준비 — 빌드와 source                                              [끝남]
───────────────────────────────────────────────────────────────────────────────
   cd ~/gold/gold_ws
   colcon build --symlink-install --packages-select white1 nxde
   source /opt/ros/humble/setup.bash
   source ~/gold/gold_ws/install/setup.bash
   ※ 터미널을 새로 열 때마다 위 두 줄을 직접 친다(.bashrc 자동화는 하지 않는다).
   ※ --symlink-install 이 아니면 paths.py 가 소스 트리(gps_data·ros2bag)를 못 찾는다.
   ros2 run white1 record --ros-args -p force_record:=true



───────────────────────────────────────────────────────────────────────────────
 1. [ ] E-STOP 안전장치 실차 확인                            ★이번 변경의 핵심★
───────────────────────────────────────────────────────────────────────────────
   책상 시험(노드 콜백 직접 호출)은 8개 항목 전부 통과했다. 아래는 사람이 실제로
   D12 를 끊어 보는 것이다. 화면은 prompt(CLI) 하나다(prompt_g 는 삭제했다).

   1-1 [ ] 자율주행(DRIVE_RUN) 중 D12 를 끊는다
           기대 · 차는 하드웨어로 즉시 정지(A·B 보드 자체 판정, ROS 무관)
                · 화면이 메뉴로 복귀, /drive_state : DRIVE_RUN → IDLE
                · /drive_event : "🚨 E-STOP — 자율주행 취소, 메뉴로 복귀"
           확인 · ros2 topic echo /drive_state   (다른 터미널에서)
   1-2 [ ] E-STOP 물린 채로 2) 주행 시작을 눌러 본다
           기대 · 거절. "🚨 주행 시작 실패 — E-STOP 체결 중이다(해제 후 다시)"
                · GUI 는 주행 버튼 자체가 비활성이어야 한다
   1-3 [ ] E-STOP 물린 채로 1) 매핑 시작
           기대 · 정상 시작(MAP_HEADING). 차가 안 구르므로 점은 안 쌓인다 —
                  ★그게 정상이다★(경로가 오염되지 않는다는 뜻)
   1-4 [ ] 매핑 중에 D12 를 끊는다
           기대 · 매핑이 ★취소되지 않는다★ (MAP_* 유지). 해제하면 그대로 이어간다
   1-5 [ ] 해제 후 2) 주행 시작
           기대 · 정상 진입(DRIVE_HEADING). "✅ E-STOP 해제 — 자율주행 가능"
   1-6 [ ] 주행 대기 중(스위치 전환 대기)에 D12 를 끊는다
           기대 · 대기가 자동 취소되고 메뉴로 돌아온다


───────────────────────────────────────────────────────────────────────────────
 2. [ ] 수동 계측 런치 — 조이스틱 / 레버 창 + 기록          [전용 런치 2개 신설]
───────────────────────────────────────────────────────────────────────────────
   ★driving 과 joystick·master 를 동시에 띄우지 말 것★
     셋 다 /cmd_vel_raw 를 20Hz 로 발행한다. driving 은 IDLE 에서도 계속 0 을
     내므로(정지 유지가 목적), 같이 띄우면 조종 명령이 매 틱 0 으로 덮인다.
     그래서 driving 을 빼고 조종 노드를 넣은 런치를 따로 만들었다. 하나만 쓴다:

     ros2 launch white1 joy.launch.py       ← 조이스틱으로 몬다(차에 타서)
     ros2 launch white1 master.launch.py    ← 마우스 레버 창으로 몬다
                                              (값을 정확히 지정·재현하기에 유리)

   둘 다 arduino · iahrs · speed · GPS · record 를 함께 띄운다. record 는
   force_record 로 도므로 ★런치하는 즉시★ 파일이 열린다(조종 노드를 잡기 전
   대기 구간도 함께 들어간다 — 나중에 잘라 보면 된다).

   기대 · ros2bag/manual-<날짜>_<시각>.csv 가 열리고 20Hz 로 쌓인다.
          Ctrl-C 로 닫는다. (책상 확인 완료 : 3.7초에 74행)
   조작 · ★D5 를 자율주행으로★ 올려야 조종 명령이 나간다(수동조종 위치에서는
          A보드가 페달만 본다). 조이스틱은 영점 뒤 SWA, master 는 발행 ON 토글.
   주의 · driving 을 안 띄웠으므로 /drive_diag 계열 열(cte_m, ref/out/meas,
          cte_i_term_deg, goal_phase …)은 통째로 빈다 — ★정상이다★.
          채워지는 것 : fix_*, imu_*, encoder_sum, speed_kmh, cmd_*, brake,
                        steer_measured, throttle_raw, vehicle_mode, estop, board_status
   주의 · 조이스틱은 E-STOP 중에는 게이트에 막힌다("E-STOP" 사유 표시). 정상이다.


───────────────────────────────────────────────────────────────────────────────
 3. [ ] 리니어 1단·코스트 감속도 실측          ★다음 기능(코너 1단 제동)의 전제★
───────────────────────────────────────────────────────────────────────────────
   2번 구성 그대로, 넓고 곧은 곳에서. 앞뒤 사람·장애물 없는 것을 먼저 확인.

   3-1 [x] a1 (리니어 1단)   a1 = ★0.62 / 0.97 / 1.05 → 평균 0.88 m/s²★
   3-2 [x] a0 (코스트)       a0 = ★0.29 / 0.54 → 평균 0.41 m/s²★
   3-3 [x] a2 (2단)          a2 = ★2.21 / 3.82 m/s²★ (종전 2.2 를 포함한다)
           로그 ros2bag/a1a2.csv · 정리 BRAKING.md (계산·정지거리·설계 영향 전부 거기)
           ⚠️ 세 값 모두 ★지령펄스가 살아 있는 상태★ 에서 잰 것이다(구동과 제동이 함께).
              1단은 구동을 끊고 다시 재야 한다 — BRAKING.md 4절 참고.
   3-4 [x] a1 재측정(구동차단) = ★1.30 m/s² (전 구간) / 램프 0.55s + 그 뒤 1.95★
           로그 ros2bag/route_20220111_123456-20260819_150223.csv 의 goal_phase=1 구간.
           조건 충족 확인 : 제동 36행 내내 drive_pulse_cmd=0, 초기 4펄스(11.9km/h).
           ⚠️ 1회뿐이다 — 3회 이상 반복은 아직. BRAKING.md 4절에 표로 정리했다.

   산출 방법 · CSV 의 fix_lat/fix_lon 으로 GPS 변위속도를 만들어 기울기를 뽑는다.
              ★speed_kmh 열을 쓰지 말 것★ — 지속 감속을 자세로 오인해 스스로
              지운다(speed.py 헤더, 2026-08-12 실차 로그로 확인된 한계).
   판정     · a1 ≥ 1.2 m/s² → 5펄스 상향 검토 가능(5항)
              a1 < 1.2 m/s² → 4펄스 유지. 1단 코너제동만 도입한다
            ★1차 실측 결과 0.88 → 미달. 5펄스 상향은 보류하고 3-4 재측정 뒤 판단★


───────────────────────────────────────────────────────────────────────────────
 4. [ ] 코너 1단 선행제동 — 구현 후 검증          (3번 결과를 보고 착수. 미구현)
───────────────────────────────────────────────────────────────────────────────
   설계 요지 · corner_speed 가 이미 아는 v_corner·gate_dist 로
               a_req = (v² − v_corner²) / (2·(gate_dist − 마진)) 을 만들고,
               a_req 가 코스트 한계(a0)를 넘으면 1단을 문다. 코너에 다가갈수록
               a_req 는 단조 증가하므로 채터링이 구조적으로 안 생긴다.
   방어      · 저속 금지선(실측 2.5펄스 미만이면 안 문다 — A보드 기동 블랭킹 재유발)
               최소 유지 0.25s / 해제 후 락아웃 0.8s / 한 코너 최대 2회 / 상한 1.5s
   검증 항목
   4-1 [ ] 코너 진입 실측속도가 v_corner 에 실제로 도달하는가 (로그의 meas vs ref)
   4-2 [ ] 한 코너에서 brake 가 몇 번 물렸나 — ★2회 이하★ 여야 한다
   4-3 [ ] 제동 중 조향이 계속 나가는가 (cmd_steer_deg 가 0 으로 끊기지 않을 것)
   4-4 [ ] 제동 해제 직후 재기동에서 엔코더 허수 카운트가 튀는가 (encoder_sum)
   4-5 [ ] max|CTE| 가 종전보다 나빠지지 않았는가


───────────────────────────────────────────────────────────────────────────────
 5. [ ] 5펄스 직선 부스트 — 4번 검증 후                                (미구현)
───────────────────────────────────────────────────────────────────────────────
   요지 · 기준은 4펄스로 두고, far preview(14m) 안에 코너가 없을 때만 5펄스를 허용.
          조건이 깨지면 즉시 4로 되돌린다(올릴 때만 느리게).
   숫자 · 5펄스(4.42m/s)에서 조향은 도로휠 14.8°(R≈4.7m)에서 포화한다.
          코스트만으로 5→2펄스 감속에는 16~27m 가 필요해 preview 14m 를 넘는다.
          → ★1단 제동 없이 5펄스로 올리면 안 된다★
   5-1 [ ] 부스트가 직선에서만 걸리는가 (로그에서 ref=5 구간의 전방곡률 확인)
   5-2 [ ] 실측이 5.5펄스를 넘는 구간이 있는가 (있으면 부스트 즉시 취소가 동작해야)
   5-3 [ ] 코너 진입 전에 4펄스로 내려오는가


───────────────────────────────────────────────────────────────────────────────
 6. [ ] map.py 로 매핑 궤적 vs 주행 궤적 비교
───────────────────────────────────────────────────────────────────────────────
   python3 ~/gold/map.py        → 창 2번 : ① gps_data/route_*.csv (파랑)
                                          ② ros2bag/*.csv        (빨강)
   볼 것 · 코너 안쪽을 파고드는가(코너컷) / 직선에서 한쪽으로 치우치는가(CTE 편향)
           출발점 부근에서 크게 벌어지는가(헤딩 초기화 품질)


───────────────────────────────────────────────────────────────────────────────
 7. 아직 실차로 확인되지 않은 값들 — 위 시험에서 함께 볼 것
───────────────────────────────────────────────────────────────────────────────
   · CORNER_MIN_PULSE = 2      코드에는 들어갔지만 이 값으로 달린 로그가 아직 없다
                               (기존 로그는 하한 1 이던 코드로 달린 것)
   · STEER_PLANT_GAIN = 1.26   하드리밋 재측정에 따른 ★환산값★ 이고 재실측이 아니다.
     STEER_UNDERSTEER = 5.17   코너에서 덜 꺾으면 plant_gain 을 올린다(더 꺾는다)
   · CTE_KI = 0.30             30초에 pot 3.2° 로 아주 느리게 들어간다.
                               cte_i_term_deg 열이 클램프(2.5°)에 붙어 살면 과소게인
   · 종점 접근(1단 제동)       ★2026-08-19 개편, 실차 미확인★ 아래 9항으로 뺐다
   · GOAL_BRAKE1_MS2 = 0.47    가정 감속도. 실측은 코너제동 로그 1건뿐이다(구동차단).
                               로그의 goal_phase=1 구간에서 바로 갱신할 수 있다


───────────────────────────────────────────────────────────────────────────────
 8. [ ] 신호등 근접도 임계 실측         ★코드(1·2단계)는 들어갔다. 남은 건 숫자다★
───────────────────────────────────────────────────────────────────────────────
   지금 tl_red_stop_min_height = 25px 는 ★근거 없는 기본값★ 이다. 실측으로 바꾼다.

   8-1 [ ] 접근 주행 1회 기록
           ros2 launch white1 master.launch.py
           · 신호등이 화면에 처음 잡히는 곳(30m 이상)에서 시작해 정지선까지 ★한 번에★
           · 4펄스 정도로 일정하게. 중간에 서지 말 것(회귀가 망가진다)
           · 화면 HUD 의 "h 42/25px" = 지금값/임계 를 눈으로도 확인
           · ⚠️ 아직 임계를 못 정했으므로 25px 에서 멀리 선다 — 정상이다
   8-2 [ ] python3 ~/gold/tl_tune.py   → 방금 CSV 선택
           k     = ______ px·m   (R² ______ , 0.9 미만이면 그 기록은 버린다)
           d_lost= ______ m      (등기구가 화면 위로 사라진 거리)
           임계 상한 = ______ px
   8-3 [ ] 오탐 하한 확인 — 같은 기록에서 tl_state=RED_FAR 인데 신호등이 아닌 것
           (미등·붉은 간판)이 잡힌 최대 tl_near_metric = ______ px
           → 임계는 ★이 값보다 크고 8-2 상한보다 작게★. 둘이 겹치면 임계로는 못 푼다
             (그때는 tl_conf 를 올리거나 ROI 를 좁힌다)
   8-4 [ ] 정한 값으로 재주행
           ros2 launch white1 master.launch.py tl_red_stop_min_height:=____
           볼 것 · tl_red_far=True 구간의 brake_level 이 ★끝까지 0★
                 · RED 전환 → brake_level=2 까지 ★0.4~0.6초★
                 · 정지 유지 중 brake_level 에 ★0 이 한 번도 없을 것★
                 · 정지 유지 중 tl_near_metric 이 ★0 이 되지 않을 것★ ←8-2 가 맞았는지
                 · 초록 전환 → brake_level=0 까지 ★1.0±0.2초★
   8-5 [ ] 8-4 에서 정지 중 tl_near_metric 이 0 이 되면 → 임계를 낮추거나
           ★카메라를 5~8° 위로 틸트★ 한다(후자가 정답. 소실거리가 절반 이하로 준다)


───────────────────────────────────────────────────────────────────────────────
 9. [ ] 정지선 앞 2단계 정지 — 시험 7단계   ★코드는 들어갔다. 남은 건 숫자다★
───────────────────────────────────────────────────────────────────────────────
   ★[2026-08-19] 판정 방식이 바뀌었다★ 화면의 행(sl_trigger_y_frac) → ★BEV 픽셀 거리★
     정지선이 보이면 sl_brake1_px 에서 ★1단 예비제동★, sl_brake2_px 에서 ★2단 확정 정지★.
     정지선을 못 보면 종전대로 즉시 2단이다(실패 모드 = 기존 동작, 그대로 유지).
     근거·설계는 CHANGELOG.md 2026-08-19 (3) 항목.

   ★실행 방법·판정 기준·기록란은 전부 STOPLINE_TEST.md 에 있다★
     gold_ws/src/white1/STOPLINE_TEST.md   (3항이 BRAKING.md 를 두는 것과 같다)
     자동 판정 :  python3 ~/gold/sl_check.py <주행 CSV> --b1 <값> --b2 <값>

   지금 ★네 값이 전부 근거 없는 기본값★ 이다(8항의 25px 과 같은 처지):
     bev_src_pts · bev_bumper_y_px · sl_brake1_px(240) · sl_brake2_px(60)

   ★1단계가 관문이다 — 정지선이 안 잡히면 나머지는 할 필요가 없다★
     구 white 실측에서 stop_line_dist 는 ★100% 미검출★ 이었다. 원인이 모델인지
     파이프라인인지 기록에 없다. 안 잡혀도 차는 종전대로 선다 —
     ★이 기능의 실패 모드가 곧 기존 동작★ 이라, 접는 데 비용이 들지 않는다.

   9-0 [ ] 단계 0  노드 기동·두 엔진 로드·클래스표 + ★카메라 보정 ON★ (책상)
   9-1 [ ] 단계 1  ★정지선이 화면에 잡히는가★ + 오검출(차체·횡단보도) 확인
   9-2 [ ] 단계 2  ★BEV 캘리브 + 두 문턱 실측 — 이 개편의 무게중심★
                   2-A 사다리꼴 : bev_src_pts = [__,__,__,__,__,__,__,__] (깊이 ___m)
                   2-B 범퍼행   : bev_bumper_y_px = ______
                   2-C 문턱     : sl_brake2_px = ______ (___m) / sl_brake1_px = ______ (___m)
   9-3 [ ] 단계 3  판정표 정적 재현(없음/대기/1단/2단/놓침 + 상한 2종 + ★단조 증가★)
   9-4 [ ] 단계 4  저속(2펄스) 접근 실차 → sl_check.py 로 4-1~4-12 판정
                   실제 정지 위치 = 정지선 ______ m 앞
   9-5 [ ] 단계 5  회귀 — 정지선 없는 신호등에서 RED→★2단 직행★ 이 0.4~0.6초인가
                   + 원본 녹화(nxde video)에 ★어안이 남아 있는가★(보정 안 됨 = 정상)
   9-6 [ ] 단계 6  자율주행(one_launch) 통합 — 매핑 중 미개입·재출발·CTE

   ⚠️ 8항과 묶여 있다 · 두 가지 이유로:
      ① 정지선 앞에 서면 ★더 가까이★ 서게 되므로 등기구가 화면 위로 벗어날 위험이
         커진다. 정지 유지 중 tl_near_metric 이 0 이 되면 8-5 대로 ★카메라를 5~8°
         위로 틸트★ 한다(임계를 낮추는 것보다 이쪽이 정답).
      ② ★[2026-08-19] 왜곡보정이 켜져 화면 스케일이 달라졌다★ alpha=0 이라 유효
         영역만 잘라 원래 크기로 늘리므로 같은 신호등이 몇 % 커 보인다 —
         ★8항의 25px 은 이제 보정된 화면 기준으로 다시 재야 한다★.


───────────────────────────────────────────────────────────────────────────────
10. [ ] 종점 접근 1단 제동 — 2026-08-19 개편분 실차 확인      ★다음 주행에서 볼 것★
───────────────────────────────────────────────────────────────────────────────
   바뀐 것 : 종점 5m 에서 2단 → ★지금 속도로 1단이 세울 수 있는 거리에서 1단★.
             4펄스면 15.8m 앞에서 물린다. 못 세운다고 계산되면 2단으로 올린다(백스톱).
   런치    : ros2 launch white1 one_launch.py            (기본값 그대로면 켜져 있다)
             되돌리려면 goal_brake_m:=5.0  /  완전히 끄려면 goal_brake_m:=0

   ── 1차 주행(0819 15:02) 결과 : ★기능은 돌았고 상수 두 개가 틀렸다★ ──
        관측 : 14.8m 에서 1단 → ★10.8m 못 미쳐 완전정지★ → 킥 1회 → 22.3초 크립
               (전체 주행 42초의 58%). 최종 정지 종점 −0.70m.
        원인 : ① a1 가정 0.47 ↔ 실측 1.30  ② 해제선 4km/h 가 arduino 해제유예
               0.5초를 안 봤다(3.78km/h 에 풀면 반드시 선다)
        조치 : a1 1.30 / 해제선 7.7km/h / 백스톱 여유 클램프 → 체결 8.1m.
               재현 시뮬로 접근 27.4초 → 11.2초. ★아래는 그 상수로 다시 볼 것★

  10-1 [ ] goal_phase 열이 ★0→1→2★ 로 한 번씩만 올라가는가
           1↔2 를 오가면 진입 조건이 흔들린 것이다. ★3 이 보이면 1단이 안 듣는 것★
  10-2 [ ] 1단 실측 감속도 a1 = ______ m/s²
           goal_phase=1 구간의 gps_kmh 기울기. ★물린 뒤 0.6초 이후에서 잴 것★
           (앞 0.54초는 리니어 행정이 올라오는 중이라 낮게 나온다)
           → 이 값을 goal_brake1_ms2 에 넣는다. 지금 가정은 0.47
  10-3 [ ] 체결 지점 = goal_dist_m 이 goal_need_m(신설 열) 을 아래로 가르는 행
           그때 속도 ______ 펄스 / 남은 거리 ______ m
  10-4 [ ] 최종 정지 위치 = 종점 ______ m   (설계 −0.85m 안팎, ★지나치면 실패★)
           1차 = −0.70m (도착 판정 0.84m 에서 2단, 0.14m 더 가고 정지)
  10-8 [ ] ★완전정지·킥이 없어야 한다★ — 1차에서 그것이 '먼저 섰다'의 정체였다
  10-9 [ ] 크립 속도 ______ km/h (1차 1.57. 1펄스 공칭 3.18 의 절반이다 —
           낮으면 크립 구간이 그만큼 길어진다. out_pulse 가 1↔2 를 오간다)
  10-5 [ ] 크립 구간이 몇 초였나 ______ s   (20초 넘으면 a1 가정이 너무 낮다 → 올린다)
  10-6 [ ] brake_level 이 1→2 로 ★한 번만★ 올라갔는가 (왕복 0회)
  10-7 [ ] 제동 중 조향이 계속 나갔는가 (cmd_steer_deg 가 0 으로 끊기지 않을 것)

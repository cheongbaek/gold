# 2026-08-18 변경 정리 — GPS 후처리 · 코너 1단 제동 · 영상 기록 · 실차 로그 분석

> 대상 : `white1` · `nxde` 패키지 (kasa 금색차)
> 상세 근거·검산은 **`gold_ws/src/white1/CHANGELOG.md`** 의 2026-08-18 항목에 있다.
> 이 문서는 **"무엇이 바뀌었고 실차에서 무엇을 먼저 확인해야 하는가"** 만 정리한 것이다.

## 이 문서를 처음 읽는다면 — 세 줄 요약

1. **`gps.py` 를 새로 세웠다.** RTK Fixed/Float 판정(종전에는 구별 자체가 안 됐다) ·
   이상치 게이트 · 5Hz fix 사이 공백을 IMU 로 메움.
2. **코너에서 리니어 1단으로 선행제동**하는 로직을 넣었다. 안전 성질(재가속 없음·코너당
   1회)은 실차에서 확인됐지만 **감속도가 가정의 절반이라 실효가 없었다** — 원인 미해결.
3. **실차 로그 4개를 분석해 두 가지를 찾았다** : `gps_node` 가 ★2개★ 떠 있었고,
   DGPS 구간에서 헤딩이 안 잡힌 원인이 품질 문턱이었다(→ 기본값을 데이터 근거로 내렸다).

---

## 커밋

| 해시 | 내용 | 비고 |
|---|---|---|
| `795d7a0` | 정지선 앞 정지 관문(`traffic_light`) + 시험절차서·판정 스크립트 | **기존 보유 작업분** (이 세션에서 손대지 않음) |
| `835ccdb` | **`gps.py` 신설** + **코너 리니어 1단 선행제동** | |
| `3a682dd` | `record` CSV 열 이름 중복 수정 + 재발 방지 가드 | `835ccdb` 의 버그 수정 |
| `12402d2` | 이 문서 최초 작성 | |
| `ab5f5c0` | **`nxde/video.py` 신설** — 인지 화면 녹화(`ros2 run nxde video`) | |
| *(이번)* | **실차 로그 분석 반영** — `min_quality` 기본값 하향 + 융합 문턱 σ 비례 + 진단 경고 | |

빌드·기동 확인 완료(`colcon build --symlink-install --packages-select white1 nxde`).

---

## 1. `gps.py` 신설 — GPS 를 driving 대신 받아 후처리한다

`/fix` + `/imu` + `/encoder` → **`/gps_fused`(20Hz)** + `/gps_quality`
**driving 은 더 이상 `/fix` 를 직접 구독하지 않는다.** (`mapping` 은 종전대로 원값)

### ① RTK Fixed / Float 판정 — `status.status` 로는 구별이 안 됐다

설치된 `nmea_navsat_driver` 소스를 직접 확인한 결과:

| GGA quality | 뜻 | `status.status` | 기본 EPE |
|---|---|---|---|
| 4 | **RTK Fixed** | **2** | 0.02 m |
| 5 | **RTK Float** | **2** ← 같다 | 4.0 m |
| 9 | WAAS | **2** ← 같다 | 3.0 m |

즉 종전 `RTK_FIXED_STATUS = 2` 판정은 이름과 달리 **"Float 이상"** 이었고, 오차 수 m
짜리 Float 이 2cm 짜리 Fixed 와 똑같이 통과했으며 **로그에도 그 차이가 남지 않았다.**

> **「2026-08-12 (6)」이 "RTK Fixed 100%" 라고 적은 것도 실은 "Float 이상 100%" 다.**
> 그 주행이 정말 2cm 좌표였는지는 지금 되짚을 수 없다.

→ `position_covariance` 의 σ 로 갈라낸다(문턱 **0.30 m**. Fixed 최악 0.10 / Float 최선
2.00 사이라 양쪽 3배 이상 여유). σ 를 기준으로 삼은 이유는 `$GST` 가 켜지면 드라이버가
**수신기 실시간 오차추정**을 그대로 쓰기 때문이다 — 우리가 알고 싶은 것은 GGA 숫자가
아니라 *"이 좌표를 몇 cm 로 믿을 수 있나"* 다.

### ② 이상치 게이트 — "말이 안 되는 값은 버린다"

| 게이트 | 판정 |
|---|---|
| **물리** | `허용 = v·Δt + ½a·Δt² + max(바닥, 3.5σ√2)` 초과 → 버림 |
| **방향** | 변위 방위가 진행축과 거의 90°(옆걸음). 전진·후진은 둘 다 허용 |
| **품질** | `Q_NONE` 은 애초에 발행하지 않음 |

**σ 를 허용량에 넣은 것이 핵심이다** — Fixed(σ 0.02)에서는 타이트하고 Float(σ 2~4)에서는
느슨해진다. **수신기가 스스로 밝힌 불확실성만큼만 관대해진다**(innovation gating).

**★탈출구가 반드시 있어야 한다★** RTK 가 재수렴하면 *진짜 위치가 몇 m 점프한다*(직전
해가 틀렸던 것이다). 게이트만 있으면 그 옳은 값을 영원히 거부하고 낡은 DR 로 달린다 —
**게이트가 없는 것보다 더 위험하다.** 연속 5회(=1초, driving 두절 타임아웃 2초보다 짧다)
기각하면 받아들이고 코스·속도·잔차 이력을 버린다.

### ③ 공백 메움(DR) — 5Hz fix 사이 3틱이 낡은 좌표를 쓰고 있었다

4.42 m/s 라면 마지막 틱에서 위치가 **최대 0.88 m 낡는다.** IMU 방향 + 엔코더 거리로 메운다.

- **방향** : GPS 코스(절대) + 자이로 적분. driving 의 헤딩을 받아오지 않는다(순환 방지).
- **거리** : 엔코더 우선(품질 나쁠 때 GPS 변위 속도는 잡음으로 ±5 m/s 급 부풀어 오른다).
- **한도 초과 시 발행을 멈춘다** — 그래야 driving 의 두절 타임아웃이 산다. `raw_age_s` 를
  실어 보내 **DR 로 메운 시간이 두절 판정을 늦추지 않게** 했다.

### ④ DEGRADED 융합 — 구현했으나 **기본 OFF**

비RTK 지속 시 fix 하나하나를 믿지 않고 **잔차(= fix − DR예측)의 중앙값**만 따라가는
구조다. *좌표를 직접 평균하지 않는 이유* : 3.5 m/s 로 달리며 1초치 좌표를 평균하면 3.5 m
뒤처진 점이 나온다. DR 로 운동을 먼저 빼야 '순수 위치오차'가 남는다.

**끈 이유** — 최대오차가 케이스별로 **+84% ~ −116%** 로 갈린다. 상관 편향(실제 비RTK
성질)에서는 평균 +24~36% 로 좋지만 백색 잡음·선회에서 최대가 무너진다. 추종 제어에서
중요한 것은 최대오차다(CTE 스파이크와 조향 급변을 만든다).

**원인은 구조적이다** — DR 의 방향 기준을 GPS 변위에서 얻는데, 비RTK 에서는 잡음이 한
샘플 이동보다 커서 **그 변위 안에 방향 정보가 없다.** 문턱을 어디에 둬도 해결되지 않는다:

| `COURSE_SIGMA_K` | 게이트 최대오차 | 융합 최대오차 |
|---|---|---|
| 2.0 (코스를 살리려 낮춤) | **−20% / −12%** | −116% / −105% |
| 6.0 (게이트를 살리려 올림) | +12% / +3% | −140% ~ −206% |

살리는 길 3가지(`gps.py` 하단 '남은 일'):
1. **driving 의 확정 헤딩을 받는다** — 단 순환이므로 *DEGRADED 진입 직전(Fixed) 값만*
   받고 이후는 자이로만 얹어야 한다.
2. **자이로 바이어스 보정** — 정지 중 LPF. 구 `white/gps_imu.py` 에 있었고 여기엔 없다.
3. **듀얼 안테나**(moving baseline) — 근본 해결.

### 시뮬레이션이 잡은 결함 5건 (전부 수정)

| # | 무엇 | 증상 |
|---|---|---|
| 1 | 잡음을 한 번만 셌다(σ√2 누락) | 정상 fix 를 **48% 기각**, 융합 −81% |
| 2 | 3σ 로 잘랐다(균일잡음 상한은 4.9σ) | 정상 꼬리를 버려 **최대오차 −77%** |
| 3 | 방향 게이트가 잡음 구간에서 판정 | 정상 fix **44% 기각**(방위가 난수인데 판정) |
| 4 | 자이로 누적을 fix 마다 리셋 | 코스가 낡으면 **그 사이 회전이 소실** → 코너에서 크게 틀림 |
| 5 | 잔차 창 rebase 누락 | 낡은 기준을 물어 **이중 계산 → 수렴 실패** |

### 검증 (1200표본 × 3시드, 실제 함수 호출 + 20Hz 발행 틱)

| 시나리오 | 평균 | p95 | **최대** |
|---|---|---|---|
| Fixed 정상주행 | +88% | +89% | 0.320→0.052 m (**+84%**) |
| 백색 σ=0.5 / 1.0 | +2% / +0% | +7% / +1% | +12% / +3% |
| 상관편향 τ=3s (실제 Float 성질) | +11% | +13% | +14% |
| **단발 8m 튐 4회** | +19% | +8% | **8.95→1.60 m (+82%)** |
| 상관편향 + R=8m 선회 | +11% | +13% | +9% |

**악화 0건.** 정상주행 +84% 는 게이트가 아니라 **DR 공백 메움**이 낸 것이다.

---

## 2. 코너 리니어 1단 선행제동 (`driving.py`)

### 왜 필요했나 — 목표펄스를 낮춰도 줄여 줄 장치가 없었다

| 4펄스(3.54) → 2펄스(1.77) | 감속도 | 필요거리 | 스캔창(14m) 점유 |
|---|---|---|---|
| **코스트 (종전 유일한 수단)** | 0.41 실측 | **11.4 m** | **82%** |
| **1단** | 0.88 실측 | **5.3 m** | 38% |
| 2단 (코너엔 못 씀) | 2.2~3.8 | 1.2~2.1 m | 15% |

게다가 `BRAKE_GATE_DECEL = 2.0` 은 **2단에서만 참인 낙관값**이라, 게이트는 코스트보다
5배 세게 줄일 수 있다고 착각한 채 목표를 냈다.

> `a1 = 0.88` 은 이미 실측돼 있었다(`todo.txt` 3-1 `[x]`, 로그 `ros2bag/a1a2.csv`).
> `BRAKING.md` 가 "재측정 전에는 못 쓴다"고 한 것은 **5펄스를 정지까지 받치는** 기준
> (`a1 ≥ 1.2`)이었고, 현 운용점은 4펄스이며 필요한 것은 정지가 아니라 2펄스 감속이다.
> 그리고 그 세 측정은 **지령펄스가 살아 있던** 값이라 **보수적 하한**이다.

### 구조 — 과거 3번 실패한 정책을 되살리지 않는다

파일 헤더의 세 번의 실패는 *실측 되먹임*('현재펄스 > 목표+3 이면 2단')이 원인이었다.
그 정책은 목표를 0 으로 덮어 A보드 기동 블랭킹을 재유발하고, 그때 쏟아지는 엔코더 허수를
'과속'으로 읽어 또 물었다. 셋 다 피한다:

| | 이 로직 |
|---|---|
| 체결 판정 | **경로 기하만**(남은 거리 + 코너 목표속도). 실측을 안 본다 |
| 해제 판정 | 실측속도(**GPS 전용**). 실측은 **푸는 방향으로만** 작용한다 |
| 목표펄스 | **0 으로 덮지 않는다**(`arduino.py` 가 어차피 덮는다 → 해제 시 전이 1회) |
| 재체결 | **코너당 1회.** 해제 후 잠금(시간 + '새 코너' 둘 다) |
| 속도 출처 | **GPS 만.** 없으면 아예 체결하지 않는다(3차 실패의 근원이 엔코더 허수) |

**2단은 주행 중 쓰지 않는다.** 부수 효과로 B보드의 '페달이 덜 빠진 채 멈춤' 위험도
줄어든다 — 0단 복귀는 위치를 안 보고 고정 1000ms REV 인데, 2단은 540카운트(850→310)로
marginal 하지만 **1단은 290카운트라 1.9배 여유**다.

### 시뮬레이션이 잡은 결함 2건 (전부 수정)

1. **해제 직후 REF 가 속도를 되돌렸다** — 2.76펄스까지 줄여 놓고 해제하자마자 2.93펄스로
   되올라갔다(목표 2). → 제동을 결정한 코너에 대해 **REF 상한을 `v_corner` 로**(`_cb_cap`).
2. **완만 코너에서 0.20초만 물렸다** — 리니어는 1단까지 290카운트(≈0.54s)를 움직여야 하니
   0.2초면 **제동력이 거의 안 나오는데 기구만 왕복**한다. → **최소 물림 0.5s** + 체결
   문턱을 그것에서 **파생**(`해제선행 0.71 + a1 0.88 × 0.5 = 1.15 m/s`).
   이 파생이 두 가지를 동시에 보장한다: ⓐ 물면 행정을 끝까지 낸다 ⓑ **목표 밑으로 안
   내려간다**(보장 한계 `a1 ≤ 2.30`, 실측 1단은 0.62~1.05).

### 검증 (급코너 4→2펄스, 리니어 행정 램프 0.54s 반영)

| a1_real | 코너 진입 | 목표 초과 | 최저속도 |
|---|---|---|---|
| **제동 OFF (종전)** | 3.79펄스 | **+1.79** | 3.79 |
| 0.62 (실측 최악) | 2.71 | +0.71 | 2.71 |
| **0.88 (실측 평균)** | **2.39** | **+0.39** | 2.39 |
| 1.30 / 2.00 / 2.50 | 2.00 | +0.00 | 2.00 |

**모든 조건에서 최저속도가 목표 미달로 내려가지 않았다 = 재가속 없음.**
연속 코너 3개에서 **코너당 정확히 1회** 체결(브레이크 전이 2회, 왕복 없음).

> **로그의 `a1` 은 정상값보다 낮게 읽힌다**(시뮬 0.73 vs 실제 0.88 = 83%) — 앞 0.54초가
> 행정 램프 구간이다. **기울기는 물린 뒤 0.6초 이후에서 재야 한다.**
> 그리고 우연히 `CORNER_BRAKE_LATE_K = 1.2` 가 그 손실(1/1.2 = 0.83)을 정확히 낸다.
> **내리지 말 것.**

---

## 3. 현재 속도 설계 (참고)

| | 펄스 | m/s | km/h |
|---|---|---|---|
| **주행 기본속도** `DRIVE_PULSE` | 4 | 3.54 | **12.7** |
| 절대 상한 `MAX_PULSE_LIMIT` | 4 | 3.54 | 12.7 |
| 헤딩 초기화 `HEADING_PULSE` | 3 | 2.65 | 9.5 |
| **코너 감속 하한** `CORNER_MIN_PULSE` | **2** | **1.77** | **6.4** |
| 종점 크립(코너와 무관) | 1 | 0.88 | 3.2 |

코너 감속은 요구 도로휠각에 선형 비례하되 **정수 펄스라 실질 3단계**다:

| 요구 도로휠각 | 회전반경 | 목표 |
|---|---|---|
| 0~3° | 24 m~ | 4펄스 12.7 km/h |
| 4~9° | 8~18 m | 3펄스 9.5 km/h |
| 10° 이상 | 7 m 이하 | 2펄스 6.4 km/h (하한) |

`CORNER_MIN_PULSE = 2` 의 근거는 실측이다 — 1펄스로 내려간 순간 코너에서 차가 아예
멈췄고(`rec_20260811_212858`, 11.7초 전진 0), 2펄스 구간이 추종이 가장 좋았다(max|CTE| 0.17m).

---

## 4. ★부수 발견 — A보드 재가속 함정★ (펌웨어 미수정)

"브레이크 해제 후 재가속이 안 된 원인"을 추적하다 찾았다.
**ROS 경로는 결백하다** — `arduino.py on_tx_timer` 는 A 페이로드가 `'0'→'4'` 로 *바뀌면
즉시* 쓴다(keepalive 대기 없음). 문제는 `kasa_0804_A.ino` 의 적분 안티와인드업 경계다:

```c
const int I_ACCUM_ERR_MAX = 4;
if (pwm > 0 && pwm < PWM_MAX && abs(err) < I_ACCUM_ERR_MAX) pidI[idx] += err;
```

**정지한 차**에 목표를 주면:

| 목표 | err | 적분 | PWM (MAX 170) |
|---|---|---|---|
| 1펄스 | 1 | 누적 O | 60 → 100 (26.7s) |
| 2펄스 | 2 | 누적 O | 71 → 111 (13.3s) |
| 3펄스 | 3 | 누적 O | 81 → **121** (8.9s) |
| **4펄스** | **4** | **★동결★** | **92 → 영원히 92 (54%)** |

`abs(err) < 4` 이므로 err 가 **정확히 4** 면 거짓 → **적분이 자라지 않는다.**
**목표 4펄스가 목표 2·3펄스보다 PWM 이 낮다.** LAUNCH 구간에서도 `launchPwm()` 이
`ffPwm`(=90)에 캡을 씌운다. → **`BRAKING.md` 부록 A 의 "4펄스 12.4초 / 엔코더 0 /
GPS 이동 0.03 m" 가 그대로 설명된다.** 기존 가설(B보드 0단 복귀가 위치 미확인 고정
1000ms → 페달이 덜 빠짐)과 **모순이 아니라 곱해진다.**

### 반직관적 결론

| 경과 | 목표 3펄스 | 목표 4펄스 |
|---|---|---|
| 0초 | 81 | **92** |
| **2.3초** | **91.5** | **91.6** ← 교차 |
| 9초 이후 | **121** | 92 (영원히) |

**목표 4는 처음 2.3초만 세고, 그 뒤로는 목표 3이 더 세다.**

### 펌웨어를 고치지 않은 이유 / 어디가 여전히 걸리나

이번 기능은 **설계로 우회했다** — 목표 2펄스 이상 유지 + 조기 해제로, 해제 시점 속도가
1.8 m/s 근처라 A보드가 보는 `useSpeed` 가 2 로 `LAUNCH_ENTRY_SPEED_MAX(1)` 을 넘어
**기동 블랭킹에 아예 들어가지 않는다.** 그래서 A/B 펌웨어 수정 없이 동작한다.

**하지만 정지 후 재출발 경로는 여전히 걸린다:**

| 경로 | 지령 | err | 판정 |
|---|---|---|---|
| 주행 시작(헤딩 초기화) | **3펄스** | 3 | **안전**(우연히 3이라 피한다) |
| **신호등 정지 → 재출발** | 4펄스 가능 | **4** | ⚠️ **위험** |
| **종점 크립 킥** (`GOAL_KICK_PULSE=4`) | **4펄스** | **4** | ⚠️ **위험** |

**ROS 쪽만으로 고칠 수 있다**(flash 불필요): 정지 상태에서 2초 안에 안 움직이면 지령을
**3펄스로 내려 적분을 쌓게** 한다. 직관과 반대라 반드시 근거와 함께 넣어야 한다.
**실차 로그로 재현을 먼저 확인한 뒤** 넣는 것을 권한다.

### 보드 분담 (혼동 방지)

| | 담당 | 이번에 관련된 이슈 |
|---|---|---|
| **A보드** `kasa_0804_A.ino` | 인휠 **주행** PID만 | 위 재가속 함정 |
| **B보드** `kasa_0813_B.ino` | **조향** DC + **리니어 제동** | 0단 복귀가 위치 미확인 고정 1000ms(2단만 marginal) |
| **`arduino.py`** (ROS) | 두 보드에 분배 | `pulse = 0 if brake > 0` — **제동 중 구동 차단은 펌웨어가 아니라 여기** |

---

## 5. `record` CSV 열 중복 수정 (`3a682dd`)

`835ccdb` 에서 `/gps_fused` 에 넣은 `gps_course_deg` 가 `/drive_diag` 가 이미 쓰던 이름과
겹쳤다. **중복은 조용히 데이터를 지운다** — 파일은 정상으로 보이고 행 수도 맞는데,
`csv.DictReader` 가 같은 키를 뒤엣것으로 덮으므로 **앞 열 값이 분석 단계에서 사라진다**
(실측: 79열 → 78키).

- `/gps_fused` 쪽을 **`gps_fix_course_deg`** 로 바꿨다. 둘은 실제로 다른 값이라 나란히
  보면 진단에 쓸모 있다:
  - `gps_course_deg` : driving 이 자기 융합에 먹인 코스(0.30 m 고정 문턱)
  - `gps_fix_course_deg` : gps.py 가 원시 fix 로 낸 코스(σ 비례 문턱)
- **재발 방지** : `ALL_COLUMNS` 중복을 import 시점에 `RuntimeError` 로 막는다.
  사람이 기억해서 피하는 게 아니라 **기동 자체를 막아** 잘못된 CSV 가 한 줄도 안 생긴다.

> ⚠️ **`835ccdb` 로 이미 기록한 CSV 가 있으면** `gps_course_deg` 가 중복된 파일이다.
> 열 번호(19번)로 읽으면 값은 살아 있다. `3a682dd` 이후 파일은 정상.

---

## 6. 기록되는 신규 열 (`record.py`, 전체 79열)

| 토픽 | 신규 열 | 무엇을 보는가 |
|---|---|---|
| `/gps_fused` (13열) | `gps_quality` | 0없음 / 1SPS / 2DGPS / **3 RTK_FLOAT / 4 RTK_FIXED** |
| | `gps_sigma_m` | **히스토그램이 두 무리로 갈려야 한다** — 그 골짜기가 0.30 문턱의 실측 근거 |
| | `gps_reject_n` | **늘어나는 구간이 GPS 가 튀는 구간** — `fix_lat/lon`(원값)과 겹쳐 볼 것 |
| | `gps_is_raw` / `gps_dr_dist_m` | 0 = IMU 로 메운 표본 / 그때 외삽한 거리 |
| | `gps_mode` / `gps_resid_m` | 0 NORMAL / 1 DEGRADED / GPS-DR 불일치 |
| `/drive_diag` (3열 추가) | `cb_state` | **0 없음 / 1 제동 / 2 잠금** |
| | `cb_v0_ms` / `cb_v_corner_ms` | 체결 시 속도 / 그때 겨눈 목표속도 |

---

## 7. `nxde/video.py` 신설 — 인지 화면 녹화 (`ab5f5c0`)

```bash
ros2 run nxde video                              # 그대로 녹화
ros2 run nxde video --ros-args -p scale:=0.5     # 용량 1/4
ros2 run nxde video --ros-args -p codec:=MJPG    # .avi (강제종료에도 앞부분 생존)
```

저장은 **`gold_ws/src/nxde/video/cam-<날짜>_<시각>.mp4`**.

- **인지가 받는 프레임 그대로** 적는다 — `imgmsg_to_cv2(desired_encoding='bgr8')` 는
  `traffic_light.cb_image` 와 같은 한 줄이다. YOLO 박스·HUD 는 **그리지 않는다**
  (판정의 *입력* 을 남기는 것이 목적).
- **어안 왜곡보정은 하지 않는다** — `camera_launch` 가 `camera_info_url` 을 일부러 주지
  않으므로 `/image_raw` 자체가 '기본 처리만 된 화면'이다. 나중에 파이프라인에 보정이
  들어가면 이 노드는 고칠 것이 없다.
- **`/dev/video2` 를 직접 열지 않는다** — V4L2 장치는 두 번 열 수 없고, 실패하는 쪽이
  `usb_cam` 이면 **신호등 인지가 죽는다**.
- **Ctrl-C · 런치 종료 모두 그 시점까지 재생 가능한 상태로 닫힌다**(둘 다 실측 확인).
- 용량 **영상 1분당 20~60MB**(1080p). 남은 디스크 500MB 밑이면 스스로 끝낸다.
- `*.mp4/avi/mkv/mov` 는 `.gitignore` 로 막았다.

---

## 8. ★실차 로그 분석 (0818 4개 주행)★ — 여기서 두 가지를 찾았다

분석 대상 : `gold_ws/src/white1/ros2bag/route_*-2026081820*.csv` 4개
(20:11 양호 / 20:18 · 20:21 · 20:25 실패)

### ⓐ `gps_node` 가 **2개** 떠 있었다 — 4개 파일 전부

`/gps_fused` 발행자가 둘이라는 것이 데이터로 증명된다:

| 검사 | 단일 노드라면 | 실제 |
|---|---|---|
| `dr_dist_m=0` 인 DR 행의 좌표 = 직전 raw 좌표 | 100% 일치 | **7% 불일치** |
| `reject_n` (단조증가만 가능) | 한 계열 | **9(271행) ↔ 4(1499행) 왕복** |
| 정지 중 `fix_course_deg`(갱신 문턱 1.0m) | 고정 | **두 계열이 각자 다른 값** |
| 같은 0.1초 창의 두 계열 좌표 거리 | 0 | **평균 5.1cm 최대 18.1cm** |

`σ`·`raw_age` 는 단일 노드 공식(`0.014 + 0.5×age`)에 완벽히 맞으므로 **코드는 정상이고
노드가 두 번 떠 있었을 뿐이다**(런치가 띄운 것 + 손으로 띄운 것으로 추정).
RTK Fixed 에서는 둘 다 같은 fix 로 스냅해 영향이 작았지만(5cm), **DGPS 에서는 각자 다르게
게이트·평균하므로 차이가 커진다.**

> **→ 주행 전에 `ros2 node list | grep -c gps_node` 가 `1` 인지 확인할 것.**
> 지금까지의 모든 로그는 두 노드 혼합이므로 그 점을 감안해 읽어야 한다.

### ⓑ DGPS 에서 헤딩을 못 잡은 원인 = 품질 문턱 (고쳤다)

| 파일 | 품질 | `gps_pos_ok` | 상태 흐름 |
|---|---|---|---|
| 20:11 | **RTK FIXED 100%** | 100% | 헤딩 확정 → **DRIVE_RUN 82초 → 도착** |
| 20:18 / 20:21 / 20:25 | **DGPS 100%** | **0%** | DRIVE_HEADING 만 → IDLE (**주행 시작 못 함**) |

`fix_status = 1` = GGA q2 = DGPS. `head_est.add()` 는 `fix_ok` 일 때만 불리고
`fix_ok = (quality >= min_quality)` 인데 **기본값이 `Q_FLOAT(3)` 이라 DGPS(2)가 막혔다.**
차는 실제로 12km/h 로 굴렀는데 **추정기에 표본이 한 개도 안 들어갔다.**
※ 이번에 생긴 회귀가 아니다 — 종전 코드도 `status >= 2` 로 막았다(DGPS 는 status=1).
달라진 것은 **이유가 로그에 보인다**는 점뿐이다.

**DGPS 로 헤딩을 잡아도 되는가 → 그 로그로 실제 `HeadingEstimator` 를 재생해 확인했다**

| 품질 | 재생 확정 | 지도 시작방위 | **차이** | σ | 잔차 |
|---|---|---|---|---|---|
| DGPS | +173.1° | +173.9° | **−0.8°** | 1.24° | 2.9cm |
| DGPS | −15.1° | −10.7° | **−4.4°** | 1.08° | 1.8cm |
| DGPS | −14.0° | −12.8° | **−1.3°** | 1.22° | 2.3cm |
| **FIXED** | −10.8° | −12.8° | **+2.0°** | 1.02° | 0.9cm |

**DGPS 의 헤딩 오차가 RTK Fixed 와 같은 급이다.** 세 건 모두 *정상확정*(강제확정 아님)이고
확정 문턱(σ 3.0° / 잔차 15cm)에도 여유가 크다 → **문턱을 손댈 필요가 없었다.**

**→ 바꾼 것 (이번 커밋)**

1. **`gps.py` `min_quality` 기본값 `Q_FLOAT(3)` → `Q_DGPS(2)`**
   `1(SPS)` 로 내리지 않은 이유 : `HeadingEstimator` 에는 자체 품질검사가 있지만
   **`forced = dist >= 5.0m` 경로가 있어 정확도 미달이어도 확정한다**. SPS(EPE 4m)면
   그 경로로 엉뚱한 헤딩이 박힌다. `2` 는 그 구멍만 막고 DGPS 는 통과시킨다.
2. **`driving.py` `FUSE_SIGMA_K = 6.0` 신설** — 위 ①을 열면 `_fuse_gps_course` 도 DGPS 로
   돌기 시작한다. 그 코스 산포를 실측해 보니:

   | 품질 | 코스 차분 표준편차 | 최대 |
   |---|---|---|
   | RTK Fixed (곡선 주행 포함) | 2.9° | 9.6° |
   | DGPS (a) / (b) | 1.6° / 5.6° | 6.5° / 20.5° |
   | **DGPS (c)** | **26.2°** | **177.1°** ← 방위가 뒤집혔다 |

   (c)는 1.33m 를 10.1초에 가는 저속 구간이다 — 느릴수록 0.30m 를 채우는 데 오래 걸려
   잡음이 쌓인다. `FUSE_GAIN(0.05)` 이라도 177° 오차는 한 스텝에 **8.9°** 를 헤딩에
   주입한다. → 필요 변위를 `max(0.30m, 6σ)` 로 : Fixed(σ0.02) **0.30m 그대로**,
   DGPS(σ0.14) **0.84m** → 방위 오차 ≈13° 로 묶인다.
   (`gps.py` 의 `COURSE_SIGMA_K` 와 같은 값·같은 이유)
3. **진단 경고 추가** — 품질 미달로 표본이 안 쌓이면 `run_heading_init` 이 사유와
   해결 명령을 그 자리에서 찍는다. 종전에는 **DRIVE_HEADING 에서 조용히 멈춰 있을 뿐**
   이라 로그를 뒤져야 원인을 알 수 있었다(이번에 겪은 그것이다).
4. `one_launch.py` 에 `min_quality` 런치 인자 노출.

⚠️ **잔차가 작은 것이 헤딩이 정확하다는 증거는 아니다.** DGPS 오차는 백색잡음이 아니라
**서행 편향**이고, 편향이 매끄럽게 흐르면 직선처럼 보여 잔차에 안 나타난다(1.1m 기선에서
10cm 횡편향 드리프트 = 5.2° 헤딩 오차인데 잔차는 작다). 위 −0.8~−4.4° 는 **결과가 좋았다는
관측**이고 σ 가 보증한 값이 아니다.

### ⓒ 양호했던 주행(20:11)의 품질 — RTK Fixed 100%

| | |
|---|---|
| **\|CTE\|** | 평균 **0.169m** / rms 0.209 / p95 0.375 / **최대 0.438m** |
| **부호반전** | **0회** (사행 없음) |
| 평균편향 | **−0.150m** (경로 오른쪽 상시 쏠림 — 기존 조향 중립 편향) |
| 속도 | 평균 7.1 / 최대 13.0 km/h |
| 도착 | `goal_dist` 최소 0.86m → **DRIVE_DONE 성립** |

**RTK 구간과 SBAS 구간의 파일 내 비교는 불가능하다** — 이 파일은 σ 최대 0.124m 로 전부
FIXED 이고, DGPS 주행 3개는 DRIVE_RUN 진입조차 못 해 CTE 데이터가 0행이다.
즉 "DGPS 에서 주행이 잘 안 된다"의 정체는 **주행이 시작되지 않았다** 이다.

### ⓓ 코너 1단 제동 — 첫 실차 데이터 : 안전은 지켰고, 효과는 안 났다

`t=19.3~20.8s`, **1.50초 물림, 코너당 1회, 잠금 11초 → 재체결 없음.** 설계대로 동작했다.

**지킨 것** ✔
- `drive_pulse_cmd = 0` — arduino 의 구동 차단 완벽 작동(위반 0행)
- 해제 2.78펄스 → 이후 최저 2.14펄스, 목표 1.63펄스 → **목표 밑으로 안 내려감 = 재가속 없음**
- `low_speed_trim` 우회 확인(제동 중 `out = ref = 2`)

**안 된 것** ✗
```
원시 fix 변위로 계산한 감속도 = 0.47 m/s²   (가정 A_BRAKE1_MS2 = 0.88)
같은 주행의 코스트             = 0.42 m/s²   → ★구별되지 않는다★
```

**리니어는 정상이다** — 같은 주행의 2단이 **3.54 m/s²** 를 냈고(BRAKING.md 2.21~3.82 범위
내), 사용자가 물리적으로도 확인했다.

그리고 `a1a2.csv` 를 **같은 방법으로** 재계산하면 1단이 처음 1.5초에
**1.02 / 2.20 / 1.86 m/s²** 다. **같은 초기속도(3.02 vs 3.03 m/s), 같은 자율주행 모드,
같은 구동 차단, 같은 발 뗀 상태**인데 우리는 0.47 이다.

**원인은 이 로그로 특정할 수 없다.** 남은 후보:
1. **B보드 전원 경합** — B보드가 조향 DC 와 리니어를 둘 다 구동한다. a1a2 는 조향
   −2~−1°(직진), 우리는 **9~13°(코너에서 조향 작동 중)**. 펌웨어 로직에 게이팅은 없으므로
   하드웨어 쪽 가능성.
2. **노면 구배** — 3% 경사 = 0.29 m/s². 배제할 수 없다.
3. 리니어가 실제로 1단 위치에 닿았는지 **로그로 확인할 방법이 없다** — B보드 텔레메트리에
   리니어 pot 이 없다(`BRAKING.md` 부록 A 2번이 이미 요청한 계측).

> **→ 그때까지 `corner_brake_enable:=false` 를 권한다.** 효과가 코스트와 같은데 리니어를
> 매 코너 왕복시킨다.

### ⓔ 부수 발견 — `MAX_PULSE_LIMIT` 를 `low_speed_trim` 이 우회한다

`out_pulse = 5` 가 47행 나왔다. `send()` 가 `MAX_PULSE_LIMIT(4)` 로 자른 뒤
`low_speed_trim` 이 `REF_TRIM_OUT_MAX(15)` 까지 올릴 수 있어서다. 기존 동작이지만
`MAX_PULSE_LIMIT` 주석의 "**파라미터로 무엇이 들어오든 이 위로는 절대 안 나간다**" 와
어긋난다. (실제 달성속도는 13.0km/h 였으므로 보정이 미달분을 메운 것 — 기능상 문제는
아니지만 문서와 코드가 다르다.)

---

## 실차 확인 순서

0. **★`ros2 node list | grep -c gps_node` 가 `1` 인가★** ← **제일 먼저.** 지금까지의
   모든 로그가 두 노드 혼합(8-ⓐ)이라 이걸 확인하지 않으면 아래가 전부 오염된다.
1. **DGPS 로 헤딩이 잡히고 주행이 되나** ← **이번 변경의 목적.** `min_quality` 기본값이
   2 라 그냥 돌리면 된다. `🧭 헤딩 확정` 이 나오는지 · `DRIVE_RUN` 에 들어가는지 ·
   \|CTE\| 가 FIXED 주행(평균 0.169 / 최대 0.438m)과 얼마나 다른지.
   안 잡히면 이제 로그가 사유를 말해 준다(`⚠️ 헤딩 표본이 쌓이지 않는다 …`).
2. **`/gps_quality` 로그의 Fixed 비율** — 기존 "Fixed 100%" 기록이 실은 "Float 이상
   100%" 였다. 진짜 비율을 먼저 안다.
3. **`gps_reject_n` 이 늘어나는 구간이 있나** → 그 시각의 `fix_lat/fix_lon` 을 보고 정말
   튄 것인지 확인. **정상인데 버렸다면 게이트가 너무 조인 것이다.**
4. **`gps_sigma_m` 분포가 두 무리로 갈리나** → `RTK_FIXED_SIGMA_M`(0.30) 의 실측 근거.
   갈리지 않으면 문턱을 다시 정해야 한다.
5. **코스 확정 로그의 기선 길이** — 출발 직진에서 1.0 m 를 확보하는지
   (`🧭 GPS 코스 확정 … (기선 N m)`).
6. **`cb_state = 1` 구간이 로그에 있나** → 없으면 문턱(1.15 m/s)이 높거나 코스가 완만한
   것. 있으면 **물린 뒤 0.6초 이후**의 `gps_kmh` 기울기가 **`a1` 실측(구동차단)** 이고,
   그 값으로 `A_BRAKE1_MS2`(현재 0.88) 를 갱신한다.
7. **해제 후 최저속도가 `cb_v_corner_ms` 밑으로 내려갔나** → 내려갔으면
   `CORNER_BRAKE_RELEASE_LEAD_MS`(0.71) 를 **키운다**. 이 방향으로 틀리면 재가속을 부른다.
8. **코너 진입 |CTE|** — 이 기능의 목적이 언더스티어 밀림 감소다. 같은 경로를
   `corner_brake_enable` true/false 로 한 번씩 달려 비교.
9. **리니어 왕복 횟수** — `brake_level` 이 1↔0 하는 빈도. 코너당 1회를 넘으면 잠금 조건
   조정.
10. **신호등 재출발·크립 킥의 성공률** — 위 A보드 함정이 실제로 재현되는지
   (`out_pulse` 는 4인데 `encoder_sum` 이 0 인 구간).

---

## 되돌리기

재빌드 없이 파라미터로 되돌릴 수 있다:

```bash
ros2 param set /driving_node corner_brake_enable false   # 코너 1단 제동 → 종전 코스트 감속
ros2 param set /gps_node     gate_enable         false   # 이상치 게이트 OFF
ros2 param set /gps_node     degraded_enable     true    # DEGRADED 융합 시험(기본 OFF)
ros2 param set /gps_node     min_quality         4       # Fixed 만 통과
ros2 param set /gps_node     min_quality         3       # 종전 기본값(Float 이상)으로 복귀
#   ※ 현재 기본값은 2(DGPS) — 8-ⓑ 의 실측 근거로 내렸다
```

`gps` 노드가 안 떠 있으면 `/gps_fused` 가 없어 **차가 서 있는다**(안전한 실패).
driving 이 그 경우를 '두절'과 **구별해서** 경고한다.

---

## 하지 않은 것

- **A/B 펌웨어 수정** — 이번 기능에 불필요(설계로 우회). 후보 3건은 `CHANGELOG.md` 의
  '하지 않은 것' 에 적었다. 다른 저장소(`mad-code`) · 유인차량 전 구간 영향 · 물리적
  flash + 재시험이 필요하므로 **별건 판단**.
- **`BRAKE_GATE_DECEL` 2.0 → 하향** — REF 프로파일까지 같이 바꾸면 나쁜 결과가 나왔을 때
  원인을 가릴 수 없다. `_cb_cap` 이 코너 부근에서 그 낙관값을 눌러 주므로 지장 없다.
- **`GOAL_KICK_PULSE = 4` → 3** — 위 표대로면 3이 더 센데, **실측 없이 바꾸지 않는다.**
- **경로 스냅(map matching)** — driving 의 경로이탈 감지(CTE 2m)를 무력화한다(진짜로
  벗어났는데 계산상 붙어 있게 된다). 하려면 이탈 감지에는 보정 전 원값을 쓰도록 분리.
- **카메라 융합** — 아직 없다. 다만 구조를 **'잔차를 먹인다'** 로 만들어 두었으므로 GPS
  잔차 자리에 차선 잔차를 같은 방식으로 얹으면 된다(구 `white` 의 `CAM_LAT_*` 가 그 구조).

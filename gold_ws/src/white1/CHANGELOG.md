# white1 변경 기록

실차 로그로 확인된 문제와 그에 대한 수정만 적는다. 각 항목은 **무엇이 관측됐는지 →
왜 그랬는지 → 무엇을 바꿨는지 → 다음에 무엇을 확인해야 하는지** 순서다.

---

## 2026-08-19 (3) — 신호등 정지를 ★2단계(1단 예비 → 2단 확정)★ 로 · 카메라 보정 공용화

> 실차 로그가 아니라 **지시사항에 따른 설계 변경**이다. 그래서 아래 '무엇이 관측됐나'
> 는 로그가 아니라 **거동의 성질**이고, 확인은 `STOPLINE_TEST.md` 로 넘긴다.

### ① 무엇이 문제였나 — 감속 프로파일이 두 상태뿐이었다

빨간불이 확정되면 **언제나 2단**이었다. 정지선이 보이면 트리거 행에 닿을 때까지
**아무 개입도 없이 참다가**(코스트) 그 순간 2단을 물었다. 즉

    무개입(코스트) ────────────→ ★급정지(2단)★

2단은 실측 **2.2~3.8 m/s²**(`BRAKING.md` 1절)라 1초 안에 세우는 힘이다. 밖에서 보면
정지선 앞에서 한 번 급하게 서다시피 한다. **같은 문제로 종점 접근제동은 이미
2026-08-19 (1) 에서 1단+거리계산으로 바뀌었다** — 신호등만 옛 태도로 남아 있었다.

### ② 무엇을 바꿨나 — 정지선이 보이면 두 문턱으로 나눠 문다

| 상황 | 종전 | 지금 |
|---|---|---|
| RED 확정 + 정지선 보임(멀다) | 무개입(코스트) | 무개입(대기) |
| RED 확정 + 정지선 ≤ `sl_brake1_px` | 무개입 | **★1단 예비제동★** |
| RED 확정 + 정지선 ≤ `sl_brake2_px` | 2단 | **2단** (그대로) |
| RED 확정 + 정지선 없음/놓침/미확정 | 2단 즉시 | **2단 즉시** (그대로) |

1단은 실측 **1.30 m/s²**(체결 뒤 0.55초는 행정 램프라 0 — `BRAKING.md` 4절)이므로
2단의 절반 이하다. **arduino 는 `brake > 0` 이면 구동펄스를 0 으로 덮으므로 1단에서도
구동은 끊긴다** — 즉 1단 예비제동은 '리니어 1/3 행정 + 구동 차단'이다.

**★단계는 올라가기만 한다★** RED 를 잡고 있는 동안 0 → 1 → 2 로만 간다. 2단을 물었다가
정지선이 흔들린다고 1단으로 내리지 않는다 — 리니어는 물리적으로 왕복하는 장치이고
그 왕복이 제일 나쁘다(2026-08-14 의 2↔0 flip-flop 과 같은 문제의식). 0 으로 돌아가는
길은 **해제 경로 하나뿐**이다(빨간불 사라짐 · 허락 상실 · 래치 ON 이면 초록불).

**★1단으로 가다 멈춰 버리면 2단으로 올린다★** 1단이면 구동이 끊겨 차가 스스로
정지선까지 못 간다. 그대로 두면 2단 문턱에 영영 못 닿으므로 `sl_wait_max_s`(**5 → 8초**)
가 차면 2단으로 승격해 정지를 확정한다(지시사항). 상한을 늘린 근거는 **종전의
'참는 동안'은 무개입이었지만 지금은 이미 1단으로 감속 중**이라는 것이다.

### ③ 판정값을 ★화면의 행 → BEV 픽셀 거리★ 로 바꿨다

문턱이 둘이 되면 화면의 행 비율(`sl_trigger_y_frac`)로는 판정할 수 없다. **원근 때문에
행 간격이 거리에 비례하지 않아** '두 문턱이 몇 m 떨어져 있나'를 말할 수 없기 때문이다.
BEV(IPM)로 펴면 **한 픽셀이 어디서나 같은 거리**라 문턱을 여럿 두어도 뜻이 산다.

    판정값 = BEV 에서 ★정지선 최근접점 → 앞범퍼★ 픽셀 거리   (가까울수록 작다)

- `sl_trigger_y_frac` **삭제** → `sl_brake1_px`(240.0) · `sl_brake2_px`(60.0) 신설
- `/tl/stop_line_px` 신설(−1 = 미검출 / 0 = 도달·지나침), CSV 열 `sl_px` 추가
- `sl_y` 는 **기록·HUD 용으로 남긴다** — 판정에는 쓰지 않는다(영상 대조용)

### ④ 어안 왜곡보정 — `camera_model.py` 신설, ★모든 카메라 노드의 단일 소유자★

BEV 를 걸려면 왜곡보정이 먼저다(어안이 남은 그림에 원근변환을 걸면 직선이 휜 채로
펴진다 — 종전 `traffic_light.py` 헤더가 '픽셀 행을 쓰는 이유'로 적어 둔 바로 그것이다).

- 계수의 정본 : **`white1/calibration/usb_cam_calibration.yaml`**(구 `white` 에서 옮겨 옴,
  `share` 로 설치). 재캘리브하면 **이 파일만** 바꾼다.
- `camera_model.declare_params(node)` + `CameraModel.from_node(node)` — 앞으로 붙일
  **차선 인지도 이 두 줄이면 같은 그림을 본다**. `camera_launch.camera_params()` 가
  런치에서 한 벌을 만들어 카메라 노드 전부에 먹인다.
- ★`/image_raw` 는 원본 그대로 둔다★ `camera_info_url` 을 주지 않고 **인지 노드 안에서**
  편다. 원본 녹화(`nxde video`)가 '카메라가 실제로 준 그림'이어야 하기 때문이다
  (지시사항 — 원본 촬영에는 보정이 필요 없다). **녹화 mp4 에 어안이 남는 것이 정상이다.**
- **fail-open** : 캘리브를 못 읽으면 경고만 남기고 보정을 끈 채 원본으로 계속 돈다.

⚠️ **호모그래피의 `w` 부호는 절대적인 뜻이 없다**(스케일까지만 정해진다). 그래서
소실선 너머의 점을 거르는 판정은 **사다리꼴 중심에서 잰 부호를 기준**으로 한다.
이것을 `w > 0` 으로 잡으면 **판정이 정확히 뒤집혀** 노면 위의 점이 전부 버려진다
(구현 중 실제로 그랬다 — 오프라인 검산에서 잡았다).

### ⑤ 다음에 확인할 것 — ★숫자는 아직 하나도 실측이 아니다★

`bev_src_pts` · `bev_bumper_y_px` · `sl_brake1_px` · `sl_brake2_px` 는 전부 **출발점**
이다(종전 `sl_trigger_y_frac=0.75` 와 같은 처지). **`STOPLINE_TEST.md` 단계 2 가
이 개편의 무게중심**이고, 순서는 사다리꼴 → 범퍼행 → 2단 문턱 → 1단 문턱이다.

그리고 **보정을 켜면 화면 스케일이 미세하게 달라진다**(alpha=0 이라 유효 영역만 잘라
원래 크기로 늘린다). 같은 신호등이 몇 % 커 보이므로 **근접도 게이트
`tl_red_stop_min_height=25px` 와 `tl_roi` 는 재실측 대상**이다(todo 8항).
연산 비용도 프레임당 **remap 3~5ms** 가 늘었다 — fps 가 25 밑으로 가면 `sl_interval:=2`.

---

## 2026-08-19 (2) — ★첫 실차 확인★ : DGPS 로 완주했다 · a1 실측 1.30 · 해제 선행 누락

> 로그 : `ros2bag/route_20220111_123456-20260819_150223.csv` (42.3초, 경로 40.5m)
> **RTK 없이 DGPS 100% 로 완주했다.** 아래 세 절은 각각 GPS·정지·헤딩이다.

### ① GPS — 2026-08-18 에 고친 것이 그대로 값을 했다

| 검사 | 결과 | 뜻 |
|---|---|---|
| 품질 | **DGPS 100%** (`fix_status=1`, σ 중앙 **0.095m**) | RTK 한 번도 안 잡혔다 |
| `gps_pos_ok` | **100%** | `min_quality=2` 가 열어 준 것이다. **3(FLOAT)이었으면 0% → 출발조차 못 했다** |
| 이상치 게이트 | `reject_n` **0** | DGPS 를 열었는데도 오검출 0 — 게이트가 과민하지 않았다 |
| fix 주기 | **5.0Hz** (최대 공백 0.25s) | DR 이 메운 구간 `dr_dist` 최대 **0.726m** |
| `gps_mode` | NORMAL 100% | DEGRADED 융합은 설계대로 꺼져 있었다 |
| **발행자 수** | `dr_dist=0` 인 256행에서 발행좌표 = 원시 fix, 오차 **0.0000m** | **`gps_node` 는 1개였다** (0818 은 7% 불일치였다) |

**즉 2026-08-18 (4) 의 두 조치가 모두 확인됐다** — 노드 중복은 사라졌고, `min_quality`
하향이 없었으면 이 주행은 시작되지 않았다.

### ② 정지 — ★상수 두 개가 실측과 어긋나 있었다★ (고쳤다)

관측된 것은 사용자가 말한 그대로다. **14.8m 앞에서 1단을 물었는데 10.8m 못 미쳐
완전히 서 버렸고**, 남은 거리를 **22.3초 동안 크립**으로 갔다(전체 주행의 58%).

**(가) `a1` 이 0.47 이 아니라 1.30 이었다** — 원시 fix 변위로 재구성:

| 구간 | 속도 | 시간 | 이동 | 감속도 |
|---|---|---|---|---|
| 물린 뒤 처음 0.55초 | 3.31 → 3.33 m/s | 0.55s | 1.83m | **0** (행정 램프) |
| 그 뒤 | 3.33 → 0.86 m/s | 1.25s | 2.11m | **1.95 m/s²** |
| **전 구간(에너지식)** | 3.31 → 0.86 m/s | 1.80s | 3.94m | **★1.30 m/s²★** |

**이것이 `BRAKING.md` 4절이 요구한 '구동을 끊고 잰 a1' 이다** — 제동 중
`drive_pulse_cmd` 는 36행 내내 0 이었다(구동 차단 확인). 0.88(구동 살아 있음)도,
0.47(0818 코너 추정)도 아니었다. 0.47 은 **행정 램프가 지배하는 0.5~1초짜리 짧은
체결**에서 나온 값이라 종점처럼 오래 무는 구간에 쓸 수 없었다.

**(나) 해제 선행이 통째로 빠져 있었다 — '처음에 멈춘' 진짜 원인**

`goal_creep_kmh=4.0` 을 그대로 해제선으로 썼는데, 로그가 그 뒤를 보여 준다:

| t | `brake_level` | `drive_pulse_cmd` | GPS |
|---|---|---|---|
| 15.71 | **0**(해제 명령) | **0** | 3.78 km/h |
| 16.11 | 0 | **1** ← 여기서야 구동이 살아났다 | 1.39 km/h |
| 16.31 | 0 | 1 | **0.36 km/h** (사실상 정지) |

`arduino` 는 0 아닌 요청 뒤 **0.5초**(`BRAKE_RELEASE_HOLD_S`)는 더 낮추지 않는다.
그동안 리니어는 물려 있고 A보드 REF 도 0 이다 — a1 1.95 면 그 사이에만 **1.0 m/s**
가 사라진다. 3.78km/h(1.05m/s)에서 풀면 **반드시 선다.**
→ 해제선 = 크립속도 + `a1×0.5 + (a1+a0)/2×0.54 + a0×0.35` = **7.7 km/h**.
※ **코너 1단 제동의 `CORNER_BRAKE_RELEASE_LEAD_MS(0.71)` 도 같은 항이 빠져 있다.**
   그때는 a1 을 0.47 로 봐서 0.24m/s 라 묻혔지만 실측 1.95 에서는 1.0m/s 다 — 별건.

**(다) ★재현 시뮬이 세 번째 결함을 잡았다★ — 백스톱이 램프 중에 물린다**

(가)를 반영해 `a1=1.30` 으로 올리자 `need₁` 이 7.2m 가 됐는데, **물린 뒤 0.55초는
감속도가 0** 이라 차가 2.1m 를 그대로 간다 → 남은 거리가 2단 문턱(5.2m)을 밟아
**1단이 듣기도 전에 백스톱이 물렸다**(= 종점 접근이 통째로 다시 2단).
→ 체결 지점을 `need₁ ≥ need₂ + v·램프 + 1.0m` 로 클램프했다. 4펄스에서 **8.1m**.
**부수 효과가 크다** — a1 이 1.1 을 넘으면 체결 지점을 이 식이 정하므로 **a1 을 정확히
못 재도 결과가 흔들리지 않는다**(a1 은 온도·노면·마모로 변한다).

### 검증 — 이 주행의 실측 물리를 넣은 ★재현 시뮬★

차량 모형은 전부 이 로그에서 뽑았다(램프 0.55s → 1.95 m/s², arduino 해제유예 0.5s,
크립 실측 0.44 m/s, 정지에서 떼려면 4펄스). 종점 로직은 **실제 함수를 그대로** 부른다.

| | 체결 | 결과 | 접근 시간 |
|---|---|---|---|
| **종전 상수(이번 실차)** | 15.7m | 10.7m 못 미쳐 **완전정지 + 킥** | **27.4초** |
| **고친 뒤** | 8.1m | 구르는 채로 크립 인계, 정지·킥 없음 | **11.2초** |

실제 a1 을 0.41(=코스트) ~ 3.5 로 흔들어도 **전부 종점 −0.85m 에 8~13초**로 선다.
1단이 약한 쪽(≤0.8)에서만 백스톱이 뜨고, 그때도 결과는 같다.

### ③ 헤딩 — RTK 없이 왜 잘 잡혔나

확정값 **−14.8°** (σ 1.08°, 잔차 **0.9cm**, 기선 1.06m, 13점).
지도 시작방위(출발 WP 11 → +2.3m)는 **−12.4°** → **차이 −2.4°**.
0818 재생값(DGPS −0.8~−4.4° / Fixed +2.0°)과 같은 급이다.

**핵심은 이 한 줄이다 — 같은 시각 GPS 절대 σ 는 0.093m 인데 직선 잔차는 0.009m 였다.**
DGPS 오차는 백색잡음이 아니라 **천천히 흐르는 공통 편향**이라, 1초짜리 기선의 **차분**
에서는 10분의 1 로 줄어든다. 헤딩은 위치가 아니라 **변위**로 만드는 값이므로 그 편향이
대부분 상쇄된다. 추종도 같은 이유로 성립한다(CTE rms 0.297m, 최대 1.125m 는 출발 정렬
구간 — 출발 WP 가 1.13m 떨어져 있었다).

⚠️ **다만 잔차가 작은 것이 헤딩이 맞다는 증거는 아니다**(편향이 매끄러우면 직선처럼
보인다). 이 주행에서 확인된 것은 **결과가 −2.4° 였다**는 관측이지 보증이 아니다.
σ 1.08° 도 실은 잔차가 아니라 `HEAD_SIGMA_FLOOR(2cm)` 가 만든 값이다.

### 다음에 무엇을 확인해야 하나

1. **고친 상수로 다시 한 번.** `goal_phase` 가 0→1→2 로 한 번씩, `brake_level` 1→2 왕복
   0회, **완전정지·킥 없이** 크립으로 넘어가는지.
2. `goal_need_m` 과 `goal_dist_m` 이 만나는 행(체결 지점)이 **8m 근처**인지.
3. 최종 정지 위치 **종점 −0.85m 안팎**인지.
4. **크립 속도가 왜 1.57 km/h 인가**(1펄스 공칭 3.18). 저속 보정이 ref 1↔2 를 오갔다 —
   크립 구간 길이가 설계의 2배가 된다.
5. **코너 1단 제동의 a1·해제선행**(위 (나) ※) — 이 로그에서는 `cb_state` 가 한 번도
   1 이 되지 않아 검증되지 않았다. 코너가 있는 경로에서 다시 볼 것.

---

## 2026-08-19 — 종점 접근을 ★리니어 1단 선행제동★ 으로 바꿨다 (+ 2단 백스톱)

### 무엇이 문제였나

사용자 지적 : **"먼저 섰다가, 서서히 기어갔다가, 완전히 멈춘다."** 로그와 숫자가
그대로 그 말이다 — 2026-08-12 의 종점 순차감속은 종점 **5m 고정**에서 **2단**을 물었고,
2단은 실측 2.2~3.8 m/s² 라 4펄스(3.54 m/s)를 **1.1초 만에** 1.11 m/s 로 떨어뜨린다.
그 뒤 남은 2.4m 를 1펄스로 기어가고, 도착에서 다시 2단으로 선다. **세 박자다.**

### 무엇을 바꿨나 — 속도로 계산한 지점에서 1단

```
종전 : 종점 5m 고정 → 2단 → 4km/h 에서 해제 → 1펄스 크립 → 도착 2단
지금 : need₁ = v·t_lag + v²/(2·a1) + margin  지점에서 → ★1단★
       → (미달이면) 크립 → 도착 즉시 ★2단★ → 완전정지 → 주행 종료
```

| 진입 속도 | 1단 체결 지점 `need₁` | (참고) 2단 백스톱 문턱 |
|---|---|---|
| 4펄스 3.54 m/s | **15.8 m** | 5.2 m |
| 3펄스 2.65 m/s | 9.4 m | 3.2 m |
| 2펄스 1.77 m/s | 4.8 m | 2.0 m |

- `t_lag = 0.55s` : GPS 속도창 중심 0.20 + 시리얼 0.05 + 리니어 1단 행정 0.54 의
  **유효분** 0.30(행정 중 제동력이 0→a1 로 올라오므로 앞 절반은 사실상 제동이 아니다).
- **감시 창(`goal_brake_m`) 기본값을 5.0 → 20.0 으로 올렸다.** need₁ 이 15.8m 라
  5m 창으로는 '이미 늦은 지점'에서만 물게 된다. `5.0` 을 주면 사실상 종전 거동이다.

### `a1 = 0.47` — 왜 코너 제동의 `A_BRAKE1_MS2(0.88)` 와 다른 값인가

**쓰이는 방향이 반대라서 일부러 따로 뒀다.**

| | 코너 제동 | **종점 제동** |
|---|---|---|
| a 를 크게 잡으면 | 늦게 물어 **코너를 넘는다**(위험) | 늦게 물어 **종점을 지난다**(위험) |
| a 를 작게 잡으면 | 일찍 물어 느려질 뿐(안전) | 일찍 서고 **크립이 메운다**(안전) |
| 그래서 쓰는 값 | 보수적 **하한** 0.88 | **최저 실측** 0.47 |

0.47 은 2026-08-18 실차의 **구동차단 1단** 실측이다. BRAKING.md 1절의 0.62~1.05 는
*지령펄스가 살아 있던* 값이라 직접 비교가 안 된다 — 즉 참값은 **0.47~1.05 사이**이고,
그 폭을 ① 낮은 쪽으로 계획하고 ② 그보다 약하면 백스톱이 받는 방식으로 처리한다.

### 2단 백스톱 — 종점 통과를 막는 마지막 방벽

제동 중 매 틱 "**지금 2단으로 세우려면 몇 m 가 필요한가**"(`need₂`, 안전계수 1.2)를
함께 계산하고, 남은 거리가 거기 닿으면 2단으로 올린다. 4펄스에서 **5.2m** 라
**2026-08-12 의 5m 2단 접근제동이 필요할 때만 자동으로 되살아나는 것**과 같다.
올리기만 하고(1→2) 내려오지 않는다. 이미 크립 속도 밑이면 올리지 않는다(그 속도의
2단 정지거리는 0.28m 다).

### 폐루프 시뮬로 잡은 결함 1건 (수정함)

**창 안으로 이미 늦게 진입하면 1단과 2단을 같은 틱에 물었다.** B보드는 진행 중인
행정이 끝나야 다음 이동을 시작하므로(`kasa_0813_B.ino updateBrake` 의
`lin_state != LIN_IDLE` 가드) **리니어를 한 번 왕복시키면서 2단이 그만큼 늦는다**.
→ 진입 시점에 `d ≤ need₂` 면 **1단을 건너뛰고 2단으로 곧장** 가게 고쳤다.

### 미달이면 크립 — 다만 ★가능하면 서기 전에 넘긴다★

사용자 요구는 '완전 정지 후 미달이면 크리핑'이고 그 경로가 기본이다(엔코더로 정지를
확인한다). 다만 **정지한 차를 다시 떼어내는 것이 이 차의 최대 난점**이라
(2026-08-12 실차에서 크립이 종점 2.49m 앞에서 **67초 굳었다**), 아직 굴러가는 중에
이미 크립 속도까지 내려왔고 종점이 1.2m 넘게 남았으면 거기서 풀어 **구르는 채로**
넘긴다. 완전히 선 경우는 종전의 크립 재출발 킥이 떼어낸다.

**정지 판정은 제동 중에는 ★엔코더★ 로 한다** — 제동 중에는 목표펄스가 0 이라 A보드
기동 블랭킹 허수가 없고, 반대로 GPS 변위 속도는 DGPS(σ 0.14m)에서 **정지 중에도
1.3km/h** 를 만들어 `GOAL_KICK_KMH(1.0)` 로는 '섰다'를 영영 못 본다. 2026-08-18 이후
DGPS 로도 주행하므로 실제로 일어날 일이다. 크립 구간은 반대로 GPS 를 본다(구동이
살아 있어 허수가 나온다).

### 부수 수정 — 코너 제동이 남의 1단을 풀 수 있었다

`_cb_release()` 는 `brake_now == BRAKE_SOFT` 를 '내 것'의 증거로 삼았는데, 종점
접근제동이 1단으로 바뀌면서 그 전제가 깨졌다. 같은 틱에 `goal_approach` 가 먼저
1단을 물면(호출 순서상 앞이다) 코너 쪽이 전제조건 붕괴로 불려 와 **남이 방금 문
종점 제동을 풀어 버린다.** → 종점이 손을 대고 있으면(`_goal_phase != NONE`) 건드리지
않게 했다.

### 검증 — ★폐루프 시뮬레이션. 실차 아님★

`goal_approach`/`_goal_to_creep`/`_goal_stopped`/`creep_pulse` 를 **그대로 호출**하고
차량만 가짜로 두어 20Hz 로 돌렸다(a1 실제값·GPS 유무·잡음·진입속도를 바꿔 가며).

| 시나리오 | 결과 |
|---|---|
| a1 실제 0.47(가정대로) | 15.7m 에서 1단 → 3.7m 에서 크립 → **종점 −0.73m** |
| a1 실제 1.05(BRAKING.md 최대) | 10.3m 에서 크립 전환 → **종점 −0.69m** |
| a1 실제 0.30(가정보다 약함) | 2.5m 에서 **백스톱** → **종점 −0.70m** |
| a1 실제 0.41(코스트와 동일) | 1단만으로 처리 → **종점 −0.72m** |
| 창 진입 때 이미 1펄스 | 제동 없이 크립 → **종점 −0.74m** |
| GPS 속도 미수신 | 최대속도로 가정 → 일찍 물고 **4.6m 못 미쳐 정지**(안전한 실패) |
| 순환 코스(직선 5m, 호길이 100m) | **발동하지 않음** |
| `goal_brake_m:=0` | 종전 거동(도착에서만 리니어) |

**전 시나리오에서 종점을 지나치지 않았다.** 못 미치는 쪽으로만 틀린다.

### 다음에 무엇을 확인해야 하나

1. **`goal_phase` 열이 0→1→2 로 한 번씩만 올라가는가.** `3` 이 보이면 1단이 안
   듣는 것이다 — 그 구간의 `gps_kmh` 기울기(물린 뒤 0.6초 이후)가 실측 a1 이고,
   그 값을 `goal_brake1_ms2` 에 넣으면 체결 지점이 따라온다.
2. **`goal_need_m`(신설 열) 과 `goal_dist_m` 이 만나는 행 = 체결 지점.** 그 행의
   속도와 최종 정지 위치를 함께 보면 a1 가정이 맞았는지 바로 나온다.
3. **최종 정지 위치.** 설계상 종점 −0.7m 안팎이다(도착 반경 0.9m + 2단 0.17m).
   지나쳤다면 백스톱이 늦은 것 → `goal_brake1_ms2` 를 낮춘다.
4. **크립 구간 길이.** 너무 길면(20초 이상) a1 가정이 너무 낮은 것이다 — 올린다.
5. `brake_level` 이 1→2 로 **한 번만** 올라가는지(왕복 0회).

---

## 2026-08-18 (4) — ★첫 실차 로그 분석★ : gps_node 2개 · DGPS 헤딩 차단 · 코너제동 0.47

**이번 항목은 추정이 아니라 실차 로그 4개(`ros2bag/route_*-2026081820*.csv`)를 분석한
결과다.** 20:11 주행은 양호(RTK Fixed 100%, 완주), 그 뒤 3개는 전부 실패했다.

### 무엇이 관측됐나

**① `/gps_fused` 발행자가 ★2개★ 였다 — 4개 파일 전부**

| 검사 | 단일 노드라면 | 실제 |
|---|---|---|
| `dr_dist_m=0` 인 DR 행의 좌표 = 직전 raw 좌표 | 100% 일치 | **7% 불일치** |
| `reject_n`(단조증가만 가능) | 한 계열 | **9(271행) ↔ 4(1499행) 왕복** |
| 정지 중 `fix_course_deg`(갱신 문턱 1.0m) | 고정 | **두 계열이 각자 다른 값** |
| 같은 0.1초 창의 두 계열 좌표 거리 | 0 | **평균 5.1cm 최대 18.1cm** |

`σ`·`raw_age` 는 단일 노드 공식(`0.014 + 0.5×age`)에 완벽히 맞는다 → **코드는 정상이고
노드가 두 번 떠 있었다.** Fixed 에서는 둘 다 같은 fix 로 스냅해 영향이 작았지만(5cm),
DGPS 에서는 각자 다르게 게이트·평균하므로 차이가 커진다.
**→ 주행 전 `ros2 node list | grep -c gps_node` 가 `1` 인지 확인할 것.**

**② 실패한 3주행은 전부 DGPS 였고, 품질 문턱이 헤딩을 막았다**

| 파일 | 품질 | `gps_pos_ok` | 상태 흐름 |
|---|---|---|---|
| 20:11 | RTK FIXED 100% | 100% | 헤딩 확정 → DRIVE_RUN 82초 → **도착** |
| 20:18 / 20:21 / 20:25 | **DGPS 100%** | **0%** | DRIVE_HEADING 만 → IDLE |

`fix_status = 1`(GGA q2 = DGPS). `head_est.add()` 는 `fix_ok` 일 때만 불리는데
`min_quality` 기본값이 `Q_FLOAT(3)` 이라 DGPS(2)가 막혀 **표본이 한 개도 안 들어갔다.**
차는 12km/h 로 굴렀다. **"주행이 안 된다"의 정체는 주행이 시작되지 않은 것이다.**
※ 이번 회귀가 아니다 — 종전도 `status >= 2` 로 막았다(DGPS 는 1). 달라진 것은 로그에
이유가 보인다는 점뿐이다.

### 무엇을 바꿨나

**ⓐ `min_quality` 기본값 `Q_FLOAT(3)` → `Q_DGPS(2)`**

근거는 추정이 아니다 — 그 로그로 **실제 `HeadingEstimator` 를 재생**해 지도 시작방위와
대조했다:

| 품질 | 재생 확정 | 지도 시작방위 | **차이** | σ | 잔차 |
|---|---|---|---|---|---|
| DGPS | +173.1° | +173.9° | **−0.8°** | 1.24° | 2.9cm |
| DGPS | −15.1° | −10.7° | **−4.4°** | 1.08° | 1.8cm |
| DGPS | −14.0° | −12.8° | **−1.3°** | 1.22° | 2.3cm |
| **FIXED** | −10.8° | −12.8° | **+2.0°** | 1.02° | 0.9cm |

**DGPS 의 헤딩 오차가 RTK Fixed 와 같은 급이다.** 세 건 모두 *정상확정*(강제확정 아님)이고
확정 문턱(σ 3.0°/잔차 15cm)에 여유가 커서 **그 문턱은 손대지 않았다.**

**`1(SPS)` 로 내리지 않은 이유** : `HeadingEstimator` 에 자체 품질검사가 있지만
`forced = dist >= HEAD_MAX_DIST_M(5.0)` 경로가 있어 **5m 를 가면 정확도 미달이어도
확정한다.** SPS(기본 EPE 4m)면 그 경로로 엉뚱한 헤딩이 박힌다. `2` 는 그 구멍만 막는다.

⚠️ **잔차가 작은 것이 헤딩이 정확하다는 증거는 아니다.** DGPS 오차는 서행 편향이라
매끄럽게 흐르면 직선처럼 보여 잔차에 안 나타난다(1.1m 기선에서 10cm 횡편향 드리프트 =
5.2° 헤딩 오차인데 잔차는 작다). 위 −0.8~−4.4° 는 **결과가 좋았다는 관측**이다.

**ⓑ `driving.py FUSE_SIGMA_K = 6.0` 신설 — ⓐ와 한 짝이다**

ⓐ를 열면 `_fuse_gps_course` 도 DGPS 로 돌기 시작한다. 그 코스 산포를 실측했다
(DRIVE_HEADING = 직진 구간이라 코스 차분은 0 에 가까워야 한다):

| 품질 | 코스 차분 표준편차 | 최대 |
|---|---|---|
| RTK Fixed (곡선 주행 포함값) | 2.9° | 9.6° |
| DGPS (a) / (b) | 1.6° / 5.6° | 6.5° / 20.5° |
| **DGPS (c)** | **26.2°** | **177.1°** ← 방위가 뒤집혔다 |

(c)는 1.33m 를 10.1초에 가는 저속 구간이다 — 느릴수록 0.30m 를 채우는 데 오래 걸려
잡음이 쌓인다. `FUSE_GAIN(0.05)` 이라도 177° 오차는 한 스텝에 **8.9°** 를 헤딩에 주입하고
반복되면 헤딩이 무작위보행한다.
→ 필요 변위를 `max(FUSE_MIN_STEP_M, 6σ)` 로 : **Fixed(σ0.02) 0.30m 그대로**,
DGPS(σ0.14) **0.84m** → 방위 오차 `atan(0.14·√2/0.84) ≈ 13°` 로 묶인다.
`gps.py` 의 `COURSE_SIGMA_K` 와 **같은 값·같은 이유**다.

**ⓒ 진단 경고 추가 (`run_heading_init`)** — 품질 미달로 표본이 안 쌓이면 사유와 해결
명령을 그 자리에서 찍는다. 종전에는 **DRIVE_HEADING 에서 조용히 멈춰 있을 뿐**이라
로그를 뒤져야 원인을 알 수 있었다. **이번에 그것 때문에 세 주행을 날렸다.**

**ⓓ `one_launch.py` 에 `min_quality` 런치 인자 노출.**

### ★코너 1단 제동 첫 실차 데이터 — 안전은 지켰고 효과는 안 났다★

`t=19.3~20.8s`, 1.50초 물림, **코너당 1회**, 잠금 11초 → 재체결 없음. 설계대로 동작.

**지킨 것** : `drive_pulse_cmd = 0`(구동 차단 완벽, 위반 0행) / 해제 2.78펄스 → 이후 최저
2.14펄스, 목표 1.63펄스 → **목표 밑으로 안 내려감 = 재가속 없음** / `low_speed_trim` 우회 확인.

**안 된 것** : 원시 fix 변위로 계산한 감속도가 **0.47 m/s²** 다(가정 0.88).
같은 주행의 코스트가 0.42 라 **구별되지 않는다.**

**리니어는 정상이다** — 같은 주행의 2단이 **3.54 m/s²**(BRAKING.md 2.21~3.82 범위 내),
사용자가 물리적으로도 확인했다. 그리고 `a1a2.csv` 를 같은 방법으로 재계산하면 1단이 처음
1.5초에 **1.02 / 2.20 / 1.86 m/s²** 다 — **같은 초기속도(3.02 vs 3.03 m/s)·같은 자율주행
모드·같은 구동차단·같은 발 뗀 상태**인데 우리는 0.47 이다.

**원인 미해결.** 남은 후보 : ① B보드 전원 경합(B보드가 조향 DC 와 리니어를 둘 다 구동 —
a1a2 는 조향 −2~−1°, 우리는 **9~13°**. 펌웨어 로직에 게이팅은 없어 하드웨어 쪽 가능성)
② 노면 구배(3% = 0.29 m/s²) ③ **리니어가 1단 위치에 실제로 닿았는지 로그로 확인할 수
없다** — B보드 텔레메트리에 리니어 pot 이 없다(`BRAKING.md` 부록 A 2번이 이미 요청한 계측).
**→ 그때까지 `corner_brake_enable:=false` 를 권한다.**

### 양호했던 주행(20:11) 품질 — 앞으로의 비교 기준

|  |  |
|---|---|
| **\|CTE\|** | 평균 **0.169m** / rms 0.209 / p95 0.375 / **최대 0.438m** |
| **부호반전** | **0회** |
| 평균편향 | **−0.150m** (경로 오른쪽 상시 쏠림 — 기존 조향 중립 편향) |
| 도착 | `goal_dist` 최소 0.86m → DRIVE_DONE 성립 |

**RTK/SBAS 구간의 파일 내 비교는 불가능하다** — 이 파일은 σ 최대 0.124m 로 전부 FIXED 이고,
DGPS 3주행은 DRIVE_RUN 진입조차 못 해 CTE 가 0행이다.

### 부수 발견 — `MAX_PULSE_LIMIT` 를 `low_speed_trim` 이 우회한다

`out_pulse = 5` 가 47행 나왔다. `send()` 가 4로 자른 뒤 `low_speed_trim` 이
`REF_TRIM_OUT_MAX(15)` 까지 올릴 수 있다. **기존 동작이지만** `MAX_PULSE_LIMIT` 주석의
"파라미터로 무엇이 들어오든 이 위로는 절대 안 나간다"와 어긋난다. 고치지 않았다(실제
달성속도는 13.0km/h 로 보정이 미달분을 메운 것 — 기능 문제는 아니고 문서가 틀렸다).

### 다음에 무엇을 확인해야 하나

1. **★`gps_node` 가 하나인지★** — 아래 전부의 전제다.
2. **DGPS 로 헤딩이 잡히고 주행이 되나** — 이번 변경의 목적. \|CTE\| 를 위 FIXED 기준
   (평균 0.169 / 최대 0.438m)과 비교한다.
3. 안 잡히면 새 경고(`⚠️ 헤딩 표본이 쌓이지 않는다 …`)가 사유를 말해 준다.
4. 코너 제동은 `false` 로 두고, 리니어 pot 계측이 생긴 뒤 다시 켠다.

---

## 2026-08-18 (3) — GPS 이상치 게이트 (+ DEGRADED 융합은 만들었으나 ★끄고 둔다★)

같은 날 (1) 에서 만든 `gps.py` 에 **"말이 안 되는 값은 버린다"** 를 넣었다. 함께
요청받은 **비RTK 지속 구간의 DR+평균 융합**도 구현했지만 **폐루프 시뮬에서 결과가
케이스마다 갈려 기본 OFF 로 둔다** — 이유는 아래 '왜 껐나'.

### 무엇을 넣었나 — 게이트 세 겹

| | 무엇을 보는가 | 근거 |
|---|---|---|
| **물리** | `허용 = v·Δt + ½a·Δt² + max(바닥, 3.5σ√2)` 를 넘는 점프 | 이 차는 0.2초에 5m 를 못 간다 |
| **방향** | 변위 방위가 진행축과 거의 90°(옆걸음) | 자동차는 옆으로 못 간다 |
| **품질** | `Q_NONE` → 애초에 발행하지 않음 | (1) 에서 이미 넣음 |

**σ 를 허용량에 넣는 것이 핵심이다.** Fixed(σ 0.02)에서는 게이트가 타이트하고
Float(σ 2~4)에서는 느슨해진다 — **수신기가 스스로 밝힌 불확실성만큼만 관대해진다**
(통계 용어로 innovation gating). 캡(5.0m)이 없으면 Float 에서 17m 까지 열려 무력해진다.

**★탈출구가 반드시 있어야 한다★** RTK 가 재수렴하면 **진짜 위치가 몇 m 점프한다**
(직전 해가 틀렸던 것이다). 게이트만 있으면 그 옳은 값을 영원히 거부하고 낡은 DR 로
달린다 — **게이트가 없는 것보다 더 위험하다.** 연속 5회(=1초, driving 의 두절
타임아웃 2초보다 짧다) 기각하면 받아들이고 코스·속도·잔차 이력을 버린다.
※ 첫 구현에서 이 탈출구가 **상수만 있고 판정에 안 들어가 있었다**(로그 문자열에만
쓰였다). 시뮬로 잡았다.

### ★시뮬레이션이 잡은 결함 4건★ (전부 수정)

1. **잡음을 한 번만 셌다** — 게이트가 보는 것은 *두 fix 사이의 거리*이고 거기엔 양쪽
   잡음이 다 들어간다(독립이므로 σ√2). 빼먹으니 σ=1 구간의 fix 를 **절반(28/58)
   기각**했고, 데이터가 굶어 융합이 **-81%** 악화됐다.
2. **3σ 로 잘랐다** — '3σ=99.7%' 는 정규분포 이야기다. 2D 벡터 차의 크기 상한은
   균일 잡음에서 **4.9σ** 다. 3σ 면 정상 꼬리를 버려 **최대오차가 -77% 악화**됐다.
   → `GATE_SIGMA_K` 3.0 → **3.5**(×√2 = 4.95σ).
3. **방향 게이트가 잡음 구간에서 정상 fix 를 기각** — σ=0.5 에서 한 샘플 이동은
   0.4m 인데 잡음차는 ±2m 다. 즉 **측정된 방위가 난수**인데 '60° 이상'으로 판정하니
   난수의 1/3 이 걸렸다. 기각된 표본들의 jump 가 1.5~2.6m(이동 0.4m)로 숫자에 드러났다.
   → 문턱을 σ 비례로(`DIR_SIGMA_K = 6.0`) = **방향 게이트는 사실상 Fixed 전용**이 된다.
   **'잡음이 크면 방위 판정을 포기한다'가 맞는 답이다.**
4. **자이로 누적을 fix 마다 리셋했다** — `psi = 코스 + 자이로적분` 인데 그 적분을
   앵커 갱신(=매 fix)마다 0 으로 되돌렸다. NORMAL(0.2초)에서는 차이가 없지만, 품질이
   나빠 코스가 몇 초간 갱신되지 않으면 **그 사이 돈 각도가 통째로 사라져** DR 이
   '몇 초 전 방향'으로 직진한다. → 리셋은 **코스가 갱신될 때만**(`_yaw_ref`).

### 사용자 지적 반영 — 출발 직진 구간에서 첫 방위를 잘 잡는다

주행은 **출발 직후 조향 0 으로 곧게 굴러가는 구간**에서 시작하고, 그때 잡은 코스가
이후 DR 방향의 **절대 기준**이 된다. 기선 길이가 그대로 정확도가 된다:

```
0.30m → atan(0.028/0.30) = 5.4°        1.00m → 1.6°
```

→ **첫 코스만 1.0m 기선을 요구**한다(`COURSE_FIRST_MIN_STEP_M`). 이후 갱신은 0.30m 로
짧게 — 코너에서 방위를 따라가야 하기 때문이다. driving 의 `HEAD_MIN_DIST_M` 도 같은
이유로 1.0m 라 두 값을 맞췄다. 확정 시 로그를 남긴다(`🧭 GPS 코스 확정 …`).

### 검증 — ★폐루프 시뮬레이션. 실차 아님★

실제 `cb_fix`/`_gate`/`_advance_dr` 등을 그대로 호출하고, 20Hz 발행 틱까지 돌려
**발행 좌표(앵커+DR)** 를 참값과 비교했다. 1200표본 × 3시드:

| 시나리오 | 평균 | p95 | **최대** | 기각 |
|---|---|---|---|---|
| Fixed σ=0.02 정상주행 | 0.154→0.019 (+88%) | +89% | 0.320→0.052 (**+84%**) | 0 |
| 백색 σ=0.5 | +2% | +7% | 1.325→1.164 (+12%) | 0 |
| 백색 σ=1.0 | +0% | +1% | +3% | 0 |
| 상관편향 τ=3s (실제 Float 성질) | +11% | +13% | 1.071→0.925 (+14%) | 0 |
| 상관편향 τ=8s | +3% | +6% | +0% | 0 |
| **단발 8m 튐 4회** | +19% | +8% | **8.948→1.596 (+82%)** | 30 |
| 상관편향 + R=8m 선회 | +11% | +13% | +9% | 0 |

**악화 0건.** 정상주행(+84%)의 개선은 게이트가 아니라 **(1) 의 DR 공백 메움**이 낸
것이다 — 5Hz fix 사이 3틱이 낡은 좌표를 쓰던 것이 사라졌다.
**단발 튐(+82%)이 이번 변경의 목적이고 그대로 달성됐다.**

### 왜 DEGRADED 융합을 껐나 — ★구조적 한계를 시뮬이 드러냈다★

구현 자체는 의도대로 동작한다. **좌표를 직접 평균하지 않고 잔차(= fix − DR예측)의
중앙값을 따라간다** — 차가 3.5 m/s 로 달리는 중에 1초치 좌표를 평균하면 3.5m 뒤처진
점이 나오므로, DR 로 운동을 먼저 빼야 '순수 위치오차'가 남는다. 사용자가 말한
"IMU 방향이 있으니 GPS 위치를 찍기 편하다"가 이 구조다. 평균이 아니라 **중앙값**인
이유는 막으려는 것이 '한두 표본의 큰 튐'이기 때문이다.
(구현 중 **창 rebase 누락**도 시뮬로 잡았다 — 보정 후 창의 잔차가 낡은 기준을 물고
있어 이중 계산이 되어 수렴하지 않았다.)

**그런데 최대오차가 케이스마다 +84% ~ −116% 로 갈린다.** 상관 편향(실제 비RTK 성질)
에서는 평균 +24~36%/p95 +15~21% 로 좋지만, 백색 잡음과 선회에서 최대가 무너진다.
추종 제어에서 중요한 것은 **최대오차**다 — 그것이 CTE 스파이크와 조향 급변을 만든다.

**원인은 방향의 출처다.** DR 의 절대 방향을 **GPS 변위(코스)** 에서 얻는데, 비RTK
구간에서는 잡음이 한 샘플 이동보다 커서 **그 변위 안에 방향 정보가 없다.** 문턱을
어디에 두든 해결되지 않는다는 것이 숫자로 나왔다:

| `COURSE_SIGMA_K` | 게이트 최대오차 | 융합 최대오차 |
|---|---|---|
| 2.0 (코스를 살리려 낮춤) | **-20% / -12%** | -116% / -105% |
| 6.0 (게이트를 살리려 올림) | +12% / +3% | (코스 낡음 → -140~-206%) |

**양쪽을 동시에 만족하는 값이 없다.** 그래서 게이트를 살리는 쪽(6.0)으로 두고 융합을
끈다. **코드는 남겨 둔다** — 방향의 출처가 좋아지면 그대로 살아난다.

### 남은 일 — 융합을 살리려면 방향의 출처를 바꿔야 한다

1. **driving 의 확정 헤딩을 받는다**(작업량 작음). driving 은 출발 직진 구간에서
   직진성 잔차까지 검사해 헤딩을 확정하고(`HeadingEstimator`) 유지한다 — 이미 우리보다
   좋은 값을 들고 있다. ⚠️ 그 헤딩은 *이 노드가 준 위치*로 만들어지므로 **순환**이다.
   끊는 법: **DEGRADED 진입 직전(Fixed 구간) 값만** 받고 그 뒤는 자이로만 얹는다.
2. **자이로 바이어스 보정**(작업량 중간). 정지 중(엔코더 0 · GPS 변위 0) `gyro_z` 를
   저역통과로 모아 빼면 된다 — 구 `white/gps_imu.py` 가 하던 것이고 여기엔 없다.
3. **듀얼 안테나**(하드웨어, 근본 해결). 수신기가 방향을 직접 준다.

### 그 외

- **DR 속도 출처에 엔코더를 추가**했다(우선순위: 엔코더 → GPS 변위). 품질이 나쁠 때
  GPS 변위 속도는 **잡음으로 부풀어 오른다**(σ 2m 짜리 두 fix 를 0.4초로 나누면 속도
  오차가 ±5 m/s 급). 엔코더는 품질과 무관하고 지연도 없다. 허수 카운트 대비로
  driving 과 같은 3점 중앙값 + 물리 상한 + DR 누적거리 상한을 겹쳐 뒀다.
- `record.py` 에 3열 추가(`gps_mode`, `gps_reject_n`, `gps_resid_m`).
  **`gps_reject_n` 이 늘어나는 구간이 GPS 가 튀는 구간이다** — `fix_lat/fix_lon`(원값)과
  겹쳐 보면 무엇을 버렸는지 보인다.
- **경로 스냅(map matching)은 하지 않았다.** 이 노드는 경로를 모르고, 그건 driving 의
  경로이탈 감지(CTE 2m)를 무력화한다 — 진짜로 벗어났는데 계산상 붙어 있게 된다.
  하려면 driving 쪽에서 이탈 감지에는 보정 전 원값을 쓰도록 분리해야 한다.
- **카메라 융합 대비**로 구조를 '잔차를 먹인다'로 만들어 뒀다 — GPS 잔차 자리에 카메라
  차선 잔차를 같은 방식으로 얹으면 된다(구 white 의 `CAM_LAT_*` 가 그 구조였다).

### 다음에 무엇을 확인해야 하나

1. **`gps_reject_n` 이 늘어나는 구간이 있나.** 있으면 그 시각의 `fix_lat/fix_lon` 을
   보고 정말 튄 것인지 확인한다 — **정상인데 버렸다면 게이트가 너무 조인 것이다.**
2. **기각률.** 시뮬에서는 튐이 없으면 0% 였다. 실차에서 몇 % 나오는지 본다.
3. **`gps_sigma_m` 분포** — Fixed/Float 두 무리로 갈리는지((1) 항목의 확인사항).
4. **코스 확정 로그의 기선 길이** — 출발 직진에서 1.0m 를 제대로 확보하는지.
5. **`degraded_enable true` 로 한 번 달려 `gps_resid_m` 을 볼 것.** 그 값이 안정적이면
   위 '남은 일' 없이도 켤 수 있다는 신호다(시뮬 잡음 모델이 실제보다 나빴다는 뜻).

---

## 2026-08-18 (2) — 코너 1단 선행제동 + ★A보드 재가속 함정 발견★

**⚠️ 실차 로그가 아니라 ①기존 실측값(todo 3항) ②펌웨어 소스 분석 ③폐루프 시뮬레이션에서
나왔다.** 실차 검증은 아직 하지 않았다 — 확인 항목은 맨 아래.

### 무엇이 문제였나 — 목표펄스를 낮춰도 차를 줄여 주는 장치가 없다

`corner_speed` 는 코너 앞에서 목표펄스를 미리 낮춘다. 그런데 그 목표에 **실제로
도달하는 수단이 코스트(자연감속)뿐**이고 실측 `a0 = 0.41 m/s²` 다.

| 4펄스(3.54) → 2펄스(1.77) | 감속도 | 필요거리 | 스캔창(14m) 점유 |
|---|---|---|---|
| **코스트 (종전 유일한 수단)** | 0.41 | **11.4 m** | **82%** |
| **1단** | 0.88 | **5.3 m** | 38% |
| 2단 (코너엔 못 씀) | 2.2~3.8 | 1.2~2.1 m | 15% |

게다가 `BRAKE_GATE_DECEL = 2.0` 은 **2단에서만 참인 값**이라(BRAKING.md 3절) 게이트는
코스트보다 **5배** 세게 줄일 수 있다고 착각한 채 목표를 낸다.

**`a1 = 0.88` 은 이미 실측돼 있었다** (todo.txt 3-1 `[x]`, 로그 `ros2bag/a1a2.csv`).
BRAKING.md 가 "재측정 전에는 못 쓴다"고 한 것은 **5펄스를 정지까지 받치는** 기준
(`a1 ≥ 1.2`)이었고, 현 운용점은 4펄스이며 필요한 것은 정지가 아니라 2펄스로의 감속이다.
그 조건이면 0.88 로 충분하다. 그리고 그 세 측정은 전부 **지령펄스가 살아 있던** 값이라
(구동 차단 시엔 더 세다) **보수적 하한**이다 — 이 값으로 계획하면 제동거리를 과대평가해
일찍 물고 목표보다 위에서 끝난다 = **재가속 없는 안전한 방향**이다.

### ★부수 발견 — A보드 재가속 함정 (`kasa_0804_A.ino`)★

"브레이크 해제 후 재가속이 안 된 원인"을 추적하다 찾았다. **ROS 경로는 결백하다** —
`arduino.py on_tx_timer` 는 A 페이로드가 `'0'→'4'` 로 **바뀌면 즉시** 쓴다(keepalive 대기
없음). 문제는 펌웨어의 적분 안티와인드업 경계다:

```c
const int I_ACCUM_ERR_MAX = 4;
if (pwm > 0 && pwm < PWM_MAX && abs(err) < I_ACCUM_ERR_MAX) pidI[idx] += err;
```

정지한 차(`speed=0`)에 목표를 주면:

| 목표 | err | 적분 | PWM | PWM_MAX(170) 대비 |
|---|---|---|---|---|
| 1펄스 | 1 | 누적 O | 60 → 100 (26.7s) | 59% |
| 2펄스 | 2 | 누적 O | 71 → 111 (13.3s) | 65% |
| 3펄스 | 3 | 누적 O | 81 → 121 (8.9s) | 71% |
| **4펄스** | **4** | **★동결★** | **92 → 영원히 92** | **54%** |

`abs(err) < 4` 이므로 err 가 **정확히 4** 면 거짓 → **적분이 자라지 않는다.**
**목표 4펄스가 목표 2펄스보다 PWM 이 낮다.** LAUNCH 구간에서도 `launchPwm()` 이
`ffPwm`(=90)에 캡을 씌운다. → **BRAKING.md 부록 A 의 "4펄스 12.4초 / 엔코더 0 /
GPS 0.03m" 가 그대로 설명된다.** 기존 가설(B보드 0단 복귀가 위치 미확인 고정 1000ms →
페달이 덜 빠짐)과 **모순이 아니라 곱해진다** — 페달이 걸려 breakaway 힘이 커진 상태에서
펌웨어가 낼 수 있는 토크가 54% 에 묶여 있었다.

**→ 그래서 이 기능은 '절대 세우지 않는' 것을 설계 제약으로 삼는다.** 목표를 2펄스 이상
유지하고 일찍 풀면 해제 시점 속도가 1.8 m/s 근처라 A보드가 보는 `useSpeed` 가 2 로
`LAUNCH_ENTRY_SPEED_MAX(1)` 을 넘어 **기동 블랭킹에 아예 들어가지 않는다** = 허수
카운트도, 재가속 함정도 없다. **펌웨어를 고치지 않고 우회한다**(아래 '하지 않은 것').

### 무엇을 바꿨나 — `corner_brake()` 신설

**구조는 종점 접근제동과 같다 — 되먹임을 만들지 않는다.** 파일 헤더의 세 번의 실패는
*실측 되먹임*('현재펄스 > 목표+3 이면 2단')이 원인이었고, 그 정책은 목표를 0 으로 덮어
LAUNCH 를 재유발하고 그때 쏟아지는 엔코더 허수를 '과속'으로 읽어 또 물었다. 셋 다 피한다:

| | 이 로직 |
|---|---|
| 체결 판정 | **경로 기하만** (남은 거리 + 코너 목표속도). 실측은 안 본다 |
| 해제 판정 | 실측속도(**GPS 전용**). 실측은 **푸는 방향으로만** 작용한다 |
| 목표펄스 | **0 으로 덮지 않는다** (arduino 가 어차피 덮는다 → 해제 시 전이 1회) |
| 재체결 | **코너당 1회.** 해제 후 잠금(시간 + '새 코너' **둘 다**) |
| 속도 출처 | **GPS 만.** `gps_ms()` 가 None 이면 아예 체결하지 않는다 |

`measured_kmh()` 를 쓰지 않는 이유: GPS 가 없으면 `/speed`(**지속 감속을 자세로 오인해
스스로 지운다** — 제동 중이 정확히 그 상황) → 엔코더(**제동 중 REF=0 이라 기동 블랭킹
허수가 섞인다** — 3차 실패에서 자기가 만든 노이즈와 싸운 그 신호)로 내려간다.

**2단은 주행 중 쓰지 않는다**(사용자 결정). 코너는 1단 전용. 부수 효과로 **부록 A 의
'페달이 덜 빠진 채 멈춤' 위험도 함께 줄어든다** — 0단 복귀는 위치를 안 보고 고정 1000ms
REV 인데, 2단은 540카운트(850→310)라 marginal 하지만 **1단은 290카운트라 1.9배 여유**다.

**해제는 예측 선행 — 세 구간의 감속도가 다르다:**

| 구간 | 시간 | 감속도 | Δv |
|---|---|---|---|
| GPS 속도창(0.4s 창 중심) + arduino TX | 0.25 s | 0.88 (제동 100%) | 0.22 |
| 리니어 후퇴(290카운트 ≈ 0.54s, 제동력 a1→a0 감소) | 0.54 s | ≈0.65 | 0.35 |
| A보드 토크 복구(PWM 슬루 0→FF(70)) | 0.35 s | 0.41 (코스트) | 0.14 |
| **합계 → `CORNER_BRAKE_RELEASE_LEAD_MS`** | | | **0.71 m/s** |

**★폐루프 시뮬이 두 개의 결함을 잡았다 (둘 다 고쳤다)★**

1. **해제 직후 REF 가 속도를 되돌린다** — `BRAKE_GATE_DECEL(2.0)` 이 낙관값이라 코너
   4m 앞에서도 REF 가 3~4펄스를 요구한다. 2.76펄스까지 줄여 놓고 **해제하자마자
   되올라가** 진입이 2.93펄스였다(목표 2). → **제동을 결정한 코너에 대해 REF 상한을
   `v_corner` 로 둔다**(`_cb_cap`, 낮추는 방향만). 이것은 게이트를 실제 감속도로 계산했을
   때 나올 값과 같으므로 새 정책이 아니라 **낙관값 보정**이다.
2. **완만한 코너(4→3펄스)에서 0.20초만 물렸다** — 리니어는 1단까지 290카운트(≈0.54s)를
   움직여야 하므로 0.2초면 행정의 40% 도 못 가서 **제동력이 거의 안 나오는데 기구는
   왕복한다**(arduino 가 막고 있는 바로 그 동작). → **최소 물림 0.5s** 를 넣고, 체결
   문턱을 그것에서 **파생**시켰다: `해제선행(0.71) + a1(0.88)×최소물림(0.5) = 1.15 m/s`.
   두 가지를 동시에 보장한다 — ⓐ 물면 행정을 끝까지 낸다 ⓑ **목표 밑으로 안 내려간다:
   보장 한계는 `a1 ≤ 문턱/최소물림 = 1.15/0.5 = 2.30 m/s²`** 이고 실측 1단은 0.62~1.05
   다(2단이 2.2~3.8) — 1단이 2.3 에 닿는 것은 행정(raw 600 vs 850)상 성립하지 않는다.
   게다가 행정 램프가 여유를 더 준다(첫 0.5초의 실제 감속량은 `a1×0.5` 의 23% 수준).
   따로 정하면 이 보장이 조용히 깨지므로 파생으로 묶었다.

### 검증 — ★폐루프 시뮬레이션. 실차 아님★

실제 `corner_brake`/`_cb_hold`/`_cb_cap` 을 그대로 호출하고, 차량 물리 + `corner_speed`
의 REF 프로파일 + **리니어 행정 램프(0.54s)** 까지 모델했다. 급코너 4→2펄스, 코너를 14m
앞에서 인지:

| a1_real | 코너 진입 | 목표 초과 | 최저속도 | 제동시간 |
|---|---|---|---|---|
| **제동 OFF (종전)** | 3.79펄스 | **+1.79** | 3.79 | — |
| 0.62 (실측 최악) | 2.71 | +0.71 | 2.71 | 2.10 s |
| **0.88 (실측 평균)** | **2.39** | **+0.39** | 2.39 | 1.95 s |
| 1.05 (실측 최선) | 2.18 | +0.18 | 2.18 | 1.80 s |
| 1.30 / 2.00 / 2.50 | 2.00 | +0.00 | 2.00 | 1.6/1.3/1.2 s |

- **초과 +1.79 → +0.39 펄스** (실측 평균 기준). 최악값(0.62)에서도 +0.71 로 절반 이하.
- **모든 경우에서 최저속도가 목표 미달로 내려가지 않았다 = 재가속 없음.** 사용자가
  요구한 "너무 느려져서 재가속하는 일이 없도록"이 성립한다.
- a1 이 예상보다 세도(2.5) 목표 밑으로 안 내려간다 — 위 ⓑ 보장이 작동한다.
- 완만 코너(4→3펄스)는 **체결하지 않는다**(제동 0.00s) — 문턱 미달. 의도한 동작이다.
- **로그의 a1 은 정상값보다 낮게 읽힌다**(시뮬 0.73 vs 실제 0.88 = 83%) — 앞 0.54초가
  행정 램프 구간이다. **기울기는 물린 뒤 0.6초 이후에서 재야 한다.** 그리고 우연히
  `CORNER_BRAKE_LATE_K = 1.2` 가 그 손실(1/1.2 = 0.83)을 정확히 내고 있다 → 내리지 말 것.

**경계 시나리오** : 코너가 갑자기 9m 앞에 나타나면 +0.42, 5m 면 +1.15(OFF 는 +1.79),
제동이 거의 안 들으면(a1=0.15) +1.70 으로 OFF 와 비슷 — **개선이 없을 뿐 악화는 없다.**

### 하지 않은 것

- **`.ino` 를 고치지 않았다.** 위 A보드 함정은 이 기능에 필요하지 않다(설계로 우회했다).
  펌웨어 수정은 ⓐ 다른 저장소(`mad-code`) ⓑ 1톤급 유인차량의 전 구간 거동에 영향
  ⓒ 물리적 flash + 재시험이 필요 → **별건으로 판단할 일이다.** 후보는 셋:
  1. `abs(err) < I_ACCUM_ERR_MAX` → **정지 중에는 적분 허용**
     (`|| speed <= LAUNCH_ENTRY_SPEED_MAX`). ⚠️ 기계적으로 막힌 차에 전류를 더 넣는
     방향이라 열 위험이 있고, ROS 쪽 크립 킥이 3회 실패 후 구동을 끊는 철학과 충돌한다.
  2. `launchPwm()` 캡을 `ffPwm` → `ffPwm + α`(시간 제한부). CHANGELOG (6)이 "필요한
     것은 속도가 아니라 **떼어내는 힘**"이라 진단한 그것이다. 더 표적화된 수정.
  3. **B보드 0단 복귀를 고정 1000ms → 위치 기준**(BRAKING.md 부록 A 3번). 2단에만
     해당하므로 이번 기능과는 무관하지만 부록 A 의 유력 원인이다.
- **`BRAKE_GATE_DECEL` 을 2.0 에서 안 내렸다.** REF 프로파일까지 같이 바꾸면 나쁜 결과가
  나왔을 때 원인을 가릴 수 없다. `_cb_cap` 이 코너 부근에서 그 낙관값을 눌러 주므로
  이번 기능에는 지장이 없다. ※ 참고로 4펄스 상한에서는 2.0 과 0.88 의 REF 차이가
  `gate_dist ≤ 3m` 구간뿐이다(상한을 5펄스로 올리면 차이가 커진다).
- **`GOAL_KICK_PULSE = 4` 를 안 건드렸다.** 위 표대로면 **4펄스(PWM 92)가 3펄스(121)보다
  약하다** — 크립 재출발 킥이 역효과일 가능성이 있다. 「(6)」의 "다음에 확인" 항목이
  아직 미검증이므로 **실측 없이 바꾸지 않는다.** 다음 주행에서 킥 성공률을 볼 것.

### 다음에 무엇을 확인해야 하나

1. **`cb_state` 가 1 인 구간이 로그에 있나** (`/drive_diag` 신설 3열). 없으면 문턱
   (1.15 m/s)이 너무 높거나 코스가 완만한 것이다.
2. **`a1` 실측(구동차단)** — `cb_state=1` 구간, **물린 뒤 0.6초 이후**의 `gps_kmh`
   기울기. 이 값으로 `A_BRAKE1_MS2`(현재 0.88)를 갱신한다. 0.88 보다 크게 나오면
   제동거리가 줄어 체결이 더 늦어진다(평균속도 ↑).
3. **해제 후 최저속도가 `cb_v_corner` 밑으로 내려갔나.** 내려갔으면 `CORNER_BRAKE_
   RELEASE_LEAD_MS`(0.71)를 키운다. **이 방향으로 틀리는 것이 재가속을 부른다.**
4. **코너 진입 |CTE|** — 이 기능의 목적이 언더스티어 밀림 감소다. 같은 경로를
   `corner_brake_enable` true/false 로 한 번씩 달려 비교할 것.
5. **리니어 왕복 횟수** — `brake_level` 이 1↔0 하는 빈도. 코너당 1회를 넘으면 잠금
   조건(`CORNER_BRAKE_LOCKOUT_S` / `CORNER_BRAKE_NEW_GATE_M`)을 조정한다.
6. **해제 직후 재가속이 실제로 되나** — 위 A보드 함정이 1.8 m/s 에서는 안 걸린다는 것이
   이 설계의 전제다. `cb_state` 2→0 구간에서 `gps_kmh` 가 다시 오르는지 확인.

---

## 2026-08-18 — GPS 후처리 노드 `gps.py` 신설 : RTK 품질 판정 + IMU 공백 메움

**⚠️ 이 항목은 실차 로그가 아니라 코드·드라이버 소스 분석에서 나왔다.** 아래 ①은
설치된 드라이버 소스를 직접 읽어 확인한 사실이고, ②는 산수다. **실차 검증은 아직
하지 않았다** — 무엇을 어떻게 재야 하는지는 맨 아래에 적었다.

### 무엇이 문제였나

**① `status.status >= 2` 는 "RTK Fixed" 가 아니었다 — "Float 이상" 이었다**

`/fix` 를 만드는 `nmea_navsat_driver` 의 소스
(`/opt/ros/humble/.../libnmea_navsat_driver/driver.py`)를 열어 보면 GGA quality →
`NavSatStatus` 매핑이 이렇다:

| GGA q | 뜻 | status.status | 기본 EPE |
|---|---|---|---|
| 1 | SPS | `STATUS_FIX` (0) | 4.0 m |
| 2 | DGPS | `STATUS_SBAS_FIX` (1) | 0.1 m |
| 4 | **RTK Fixed** | `STATUS_GBAS_FIX` (**2**) | **0.02 m** |
| 5 | **RTK Float** | `STATUS_GBAS_FIX` (**2**) | **4.0 m** |
| 9 | WAAS | `STATUS_GBAS_FIX` (**2**) | 3.0 m |

즉 종전 `driving.py` 의 `RTK_FIXED_STATUS = 2` 판정은 이름과 달리 **오차 수 m 짜리
Float 을 2cm 짜리 Fixed 와 똑같이 통과시키고 있었고, 그 차이가 로그에도 전혀
남지 않았다.** 「2026-08-12 (6)」이 "RTK Fixed 100%" 라고 적은 것도 실은
**"Float 이상 100%"** 였다 — 그 주행이 정말 2cm 좌표였는지는 지금 되짚을 수 없다.

**구별할 값은 같은 메시지 안에 이미 있었다.** 같은 드라이버가
`position_covariance[0] = (HDOP × lon_std_dev)²`, `[4]` 는 위도 쪽을 채운다.
σ = √cov 로 환산하면 Fixed 는 최악(HDOP 5)에도 0.10 m, Float 은 최선(HDOP 0.5)에도
2.00 m 다. **그 사이가 비어 있어서 문턱 하나로 갈린다.**

**② 5Hz fix 사이 3틱이 같은 좌표를 재사용하고 있었다**

제어는 20Hz(0.05초)인데 fix 는 5Hz(0.2초)다. 그 사이 3틱은 좌표가 갱신되지 않으므로
**4.42 m/s 에서 마지막 틱의 좌표는 최대 0.88 m 낡아 있다.** 순수추종은 그 좌표로
목표점 방위(alpha)와 CTE 를 계산한다.

**③ NO FIX 해(解)로도 추종이 계속됐다 (부수적으로 발견)**

종전 `cb_fix` 는 `status` 를 위치 수용에 쓰지 않고 `fix_ok` 플래그에만 썼다. 그래서
`STATUS_NO_FIX` 가 와도 `self.x/self.y` 를 갱신하고 **`fix_time` 까지 리셋했다** →
두절로도 안 잡히고 좌표는 쓰레기인 채 추종이 계속되는 구간이 원리적으로 가능했다.

### 무엇을 바꿨나

**`white1/gps.py` 신설.** `/fix` + `/imu` → `/gps_fused`(20Hz) + `/gps_quality`.
`driving.py` 는 이제 `/fix` 를 직접 구독하지 않는다.

**★상시 보정이 아니다★** 구 `white/gps_imu.py` 는 상보필터로 계속 융합했지만(α
블렌딩·DR 폴백·안테나 투영·GPS 코스 위상정렬) 여기서는 하지 않는다. 규칙은 하나다 —
**원시 fix 가 오면 그 값으로 그냥 스냅한다(블렌딩 없이 덮어쓴다).** IMU 는 fix 가
아직 안 온 시간만 메운다. 그래서 나가는 좌표는 언제나 `최근 원시 fix + 그 뒤 경과
시간만큼의 추측항법` 이고, 두 성분이 `is_raw`·`dr_dist_m` 로 **분리되어** 나간다.

**① 품질 판정** — σ 를 1차 기준, `status.status` 를 상한 게이트로 쓴다.

```
Q_NONE(0) / Q_SPS(1) / Q_DGPS(2) / Q_FLOAT(3) / Q_FIXED(4)
status>=2 이고 σ ≤ RTK_FIXED_SIGMA_M(0.30m)  →  Q_FIXED, 아니면 Q_FLOAT
```

- **왜 q4 를 알아내는 게 아니라 σ 를 보는가** : 수신기가 `$GST` 를 내면 드라이버가
  위 고정 표 대신 **수신기 실시간 오차추정**을 covariance 에 넣는다. 우리가 알고
  싶은 것은 'GGA 숫자'가 아니라 **'이 좌표를 몇 cm 로 믿나'** 이므로 σ 가 질문에 더
  정확히 답한다.
- **covariance 가 비어 있으면 Fixed 로 올리지 않는다.** σ=0 을 '아주 정확'으로 읽으면
  최악의 입력을 최선으로 오해한다 — 모르면 낮게 본다.

**② 공백 메움(DR)** — `ψ = (마지막 GPS 코스) + ∫자이로 dt`, `v = 마지막 GPS 변위
속도`, `가상좌표 = 마지막 fix + ∫v·[cosψ,sinψ]dt`.

- **절대 헤딩을 driving 에서 받아오지 않는다.** 그러면 driving(헤딩)→gps(위치)→
  driving 순환이 생긴다. GPS 코스를 절대 기준으로 삼고 그 뒤 회전만 자이로로 얹어
  이 노드 혼자 닫는다.
- **속도에 엔코더를 쓰지 않는다.** A보드 기동 블랭킹 허수 카운트(「(6)」에서 실측
  최대 77카운트) 로 외삽하면 서 있는 차가 가상좌표상 기어간다.
- **한도 3개** : `DR_MAX_S`(1.0s) / `DR_MAX_DIST_M`(3.0m) / IMU 신선도(0.3s).
  넘으면 **메우지 않는다** — 틀리게 메우면 오차의 크기도 방향도 알 수 없게 되는데,
  안 메우면 오차 상한이 `v·Δt` 로 뻔하다.

**③ 두절 감지를 살려 두기 위한 두 장치** ← **이 변경에서 가장 조심한 부분이다**

DR 로 20Hz 를 계속 내보내면 `driving` 의 `now − fix_time > GPS_TIMEOUT_S(2.0s)`
타이머가 **영원히 리셋되어 두절을 못 잡는다.** 그래서:

- `gps.py` 는 DR 한도를 넘으면 **발행을 멈춘다**(조용히).
- 배열에 `raw_age_s` 를 실어 보내고 `driving` 이 `fix_time = now − raw_age_s` 로
  역산한다 → **DR 로 메운 시간이 두절 판정을 늦추지 않는다.**

**④ Q_NONE 은 발행하지 않는다** (위 문제 ③ 수정) → driving 이 2초 뒤 두절로 판단해
선다. **종전 동작을 의도적으로 바꾼 것이다.**

### driving.py 쪽 변경 — "종전 거동 유지"를 어디서 지켰나

| 항목 | 처리 |
|---|---|
| 위치(x, y) | 원시·DR **둘 다** 쓴다 (이 변경의 목적) |
| `head_est`(헤딩 초기화) | **원시만** — DR 표본은 서로 상관돼 n 만 늘려 σ 를 실제보다 좋게 보이게 만든다 |
| `_fuse_gps_course` | **원시만** — 0.30m 거리 게이트라 주기와 무관 → **거동이 종전과 완전히 동일하다** |
| `require_rtk` 게이트 | `gps.py` 의 `min_quality` **기본값 `Q_FLOAT` 로 그대로 이어받았다** (= 종전 `status>=2` 와 동일) |
| GPS 변위 속도 | 계산 위치만 `gps.py` 로 이동. **원시 5Hz fix 3점 0.4초 창 양끝점** — 식·창·값 전부 그대로 |

**`min_quality` 를 4(FIXED)로 올리지 않은 이유** : Float 밖에 안 잡히는 장소에서
헤딩 확정·코스 융합이 멈춰 **차가 출발하지 않는다.** 안전한 실패지만 조용한 실패다.
품질이 이제 로그·record 로 계속 나오니, **실차에서 Fixed 비율을 먼저 확인한 뒤**
올릴 것 (`ros2 param set /gps_node min_quality 4`).

### 검증 — ★수식 시뮬레이션까지다. 실차는 아직 아니다★

**① σ 문턱 0.30 m 이 세 등급을 겹침 없이 가른다** (드라이버 기본 EPE 표 × HDOP 0.5~5.0)

| GGA q | σ 범위 | 판정 |
|---|---|---|
| q4 Fixed | 0.010 ~ 0.100 m | FIXED |
| q5 Float | 2.000 ~ 20.000 m | FLOAT |
| q9 WAAS | 1.500 ~ 15.000 m | FLOAT |

문턱 위아래로 **3배 이상 여유**가 있다. ⚠️ 단 이것은 `$GST` 가 없을 때의 고정 표
기준이다 — GST 가 켜져 있으면 실측 σ 분포로 다시 확인해야 한다(아래 2번).

**② DR 이 실제로 오차를 줄이는가** — `_advance_dr` 수식을 그대로 옮겨 최악 조건
(R=5m 코너, v=4.42 m/s = 상한, 5Hz fix / 20Hz 제어)에서 폐루프 없이 위치오차만 계산.
**정착 후(t≥0.5s) 구간 비교** :

| 조건 | 최대 오차 | 평균 오차 |
|---|---|---|
| **DR OFF (종전 동작)** | **0.663 m** | 0.446 m |
| **DR ON** | **0.059 m** | 0.039 m |
| DR ON + 자이로 바이어스 1°/s | 0.058 m | 0.039 m |
| DR ON + 속도 10% 과대오독 | 0.090 m | 0.061 m |

- 최대 오차가 **0.663 → 0.059 m (11배)**. 0.663 은 `v·(1/5Hz − 1/20Hz)` = 정확히
  '마지막 틱에서 좌표가 낡아 있던 양'이고, 이것이 문제 ②의 정량이다.
- **자이로 바이어스·속도 오독에 둔감하다** — 매 fix 마다 누적이 0 으로 리셋되므로
  오차가 쌓일 시간이 0.2초뿐이다. 이것이 '상시 보정을 하지 않는' 설계의 이득이다.
- **DR 이 못 메우는 구간은 출발 직후 3틱뿐이다**(t=0.05/0.10/0.15). 코스를 얻으려면
  fix 두 개와 0.30 m 이동이 필요해서다 — 설계대로이고, 그 구간은 어차피 헤딩 초기화
  중이라 추종을 하지 않는다.
- ⚠️ fix 를 절반 유실(2.5Hz)시키면 최대 1.541 m 로 벌어진다. `DR_MAX_S`(1.0s)가
  버티는 구간이지만 **정확하지는 않다** — 그래서 σ 를 부풀려 내보낸다.

### 그 외

- **매핑은 손대지 않았다** (사용자 결정). `mapping.py` 는 종전대로 `/fix` **원값**을
  직접 받는다 — 경로는 '차가 실제로 지나간 자리'여야 하고, DR 가상좌표를 지도에
  굽는 것은 **추정을 사실로 굳혀** 나중에 되짚을 수 없게 만든다.
- **품질 변화를 사람에게 알린다** : `gps.py` 로그 + `driving` 의 `/drive_event`
  (Fixed↔Float 전이). 주행 시작 시점 품질도 한 줄 남긴다 — |CTE| 가 나쁜 주행을 볼 때
  **제어를 의심할지 좌표를 의심할지가 그 한 줄에서 갈린다.**
- `record.py` 에 `/gps_fused` 10열 추가. `/fix` 와 나란히 놓고 봐야 뜻이 있다.
- `one_launch.py` 에만 노드를 추가했다. `master/joy.launch.py` 는 `driving` 을
  띄우지 않으므로 건드리지 않았다.
- **`gps.py` 가 안 떠 있으면** `/gps_fused` 가 없어 차가 서 있는다(안전한 실패).
  '두절'과 '노드 없음'은 증상이 같으므로 `driving` 이 **구별해서** 경고한다.

### 다음에 무엇을 확인해야 하나  ★전부 미검증이다★

1. **Fixed 비율** — `/gps_quality` 로그. 종전 "Fixed 100%" 가 실제로 몇 %였는지.
2. **σ 히스토그램** (`record` 의 `gps_sigma_m`) — **두 무리로 갈려야 하고 그 골짜기가
   `RTK_FIXED_SIGMA_M` 의 실측 근거다.** 갈리지 않으면 0.30 을 다시 정해야 한다.
3. **DR 오차 실측** — `gps_is_raw=1` 행에서 직전 DR 예측좌표와의 거리. 이 값으로
   `DR_SIGMA_GROWTH_M_PER_S`(현재 0.5, **근거 없는 보수적 추정값**)를 대체한다.
4. **`gps_dr_dist_m` 최대값** — 0.2초 공백이면 `속도×0.2` 근처여야 한다. 그보다 크면
   fix 를 놓치고 있다는 뜻이다.
5. **|CTE| 가 개선됐나** — 문제 ②(0.88m 낡은 좌표)가 실제로 추종에 영향을 줬는지는
   아직 모른다. 같은 경로를 전/후로 달려 비교할 것.
6. **아직 하지 않은 것** : ⓐ 안테나→뒷차축 투영(구 `white` 에는 있고 white1 에는
   없다 — 회전 중 `ω·d` 횡속도와 곡선 `d²/2R` 오프셋이 그대로 남아 있다)
   ⓑ GPS 솔루션 지연(~0.2s) 보상 ⓒ 칼만/EKF — 위 1~4 를 재기 전에는 R·Q 를 정할
   근거가 없다.

---

## 2026-08-12 (6) — 실차 2주행 반영 : 속도의 출처를 GPS 로, 크립 재출발 킥

`route_20260812_162235` 를 두 번 달린 로그((5)의 첫 실차)를 대조한 결과다.
추종 자체는 양호했다 — 163m, RTK Fixed 100%, |CTE| 최대 0.94/0.70m, 전 구간 1.0m 이내.
**바꾼 것은 (5)에서 넣은 종점 순차감속 하나뿐이고, 두 실패의 원인은 같았다.**

### 무엇이 관측됐나 — /speed 로는 감속을 볼 수 없다

| 주행 | /speed 가 말한 것 | GPS 실측 | 결과 |
|---|---|---|---|
| 16:40:07 | 4.10 km/h → **해제** | **7.44 km/h** | 7km/h 로 종점 통과, 1.40m 뒤 정지. 크립은 0.15초뿐 |
| 16:44:38 | 감속 2.4초 내내 **3.75~4.04 고정** | 2.81 → 0.33 km/h | 종점 2.49m 앞에 서고 **67초 정체** |

`speed.py` 헤더가 스스로 밝힌 한계 그대로다 — 지속 감속을 자세로 오인해 신호를 지우고,
정확한 것은 '서 있는가'뿐이다. **감속 임계에 쓸 값이 아니었다.**

16:44 의 정체는 그 다음 단계까지 드러냈다. 저속 보정은 제 일을 했지만(ref 1 → out 2)
**정지한 차는 2펄스로 움직이지 않는다** — 67초 동안 GPS 이동 0.0m, 그동안 엔코더만
허수 카운트를 뱉었다(정상 4~6, 최대 **77**). 안 움직이는 차에 계속 전류를 넣은 셈이다.

### 무엇을 바꿨나

**① 속도의 출처를 GPS 변위로 (`measured_kmh`)** — 5Hz fix 3점(0.4초 창)의 양 끝점
직선거리를 시간으로 나눈다. 구간마다 더하지 않는 이유는 RTK 지터(±2cm)가 매 구간
양수로 쌓여 정지 중에도 속도가 뜨기 때문이다. 우선순위는 **GPS → /speed → 엔코더**.

검증(합성 표본, 지터 ±2cm 포함): 정지 → 0.13 km/h, 크립 3.20 → 3.49, 접근 7.40 → 7.40.

이 값은 종점 감속 임계뿐 아니라 **저속 펄스 보정과 CTE 적분 동결 판정도 같이 쓴다**
(`measured_pulse` 가 이제 `measured_kmh` 하나로 모인다). 종전에는 셋이 각자 틀린 값을
봤다 — 특히 CTE 적분의 '안 구르면 동결'(`CTE_I_MIN_PULSE` 0.3펄스)은 엔코더 허수
25펄스나 /speed 의 굳은 3.9 앞에서 아예 작동하지 않았다. GPS 기준 정지는 0.04펄스다.

**② 크립 재출발 킥** — 크립 중 1.0 km/h 아래가 0.7초 이어지면 **4펄스를 0.6초** 넣고
1펄스로 돌아온다. 3회까지 시도하고 그래도 못 움직이면 **구동을 끊고** 사람을 부른다.

- 크립 목표를 2펄스로 올리는 처방이 아닌 이유 : 그러면 접근속도가 6.4km/h 가 되어
  (5)에서 없앤 오버런이 그대로 돌아온다. 필요한 것은 속도가 아니라 **떼어내는 힘**이다.
- 킥은 저속 보정을 우회한다 — 안 그러면 ref 4 + 실측 0 → out 6 이 되어 종점 2m 앞에서
  낼 속도가 아니다.
- **도착으로 간주하지 않는다.** 종점 판정을 흔드는 변경은 별개로 검토할 일이다.

### 하지 않은 것 (분석에서 제안했으나 이번에 넣지 않음)

- **크립 정체 워치독(도착 처리)** — 위 ②가 구동을 끊는 데서 멈춘다. 3회 실패 뒤에는
  여전히 사람이 스위치를 내려야 한다.
- **종점 조향 특이점** — `target_idx` 가 마지막 WP 에 못 박히면 순수추종 이득이
  1/dist 로 발산한다. 16:40 에서 종점 2.4m→1.1m 구간에 지령이 −2.5° → **−11.6°** 로
  튀었고(변화율 RMS 0.10 → 2.83 °/틱, 28배), 이것이 '도착 직전 불안'의 정체다.
  실측 −11.59° 는 `atan(2L·sin3.07°/1.10)×1.26 + 적분 −2.40°×1.26` 으로 재현된다.
  처방은 마지막 WP 너머로 가상 연장점을 두는 것 — 추종 기하를 건드리므로 분리했다.
- **조향 중립 보정** — CTE 평균 −0.267/−0.233m, 적분이 상시 −1.1~−1.6°(클램프 −2.5°
  코앞)를 쓰고 있다. (3)에서 예측한 '중심 오프셋'이 실측으로 확인된 셈이다.
  ※ 이후 B보드 하드리밋이 684/394 로 재측정되어 **중심이 66카운트 옮겨졌다**. 스팬은
  290 그대로라 `steer_plant_gain`(1.26)은 유효하다. 편향은 다음 주행에서 다시 본다.

### 다음에 무엇을 확인해야 하나

- 해제 시점의 GPS 속도가 4 km/h 근처인지(로그의 `goal_phase` 1→2 전이 시각).
- 최종 정지 위치. 16:40 은 종점 +1.40m 였고, 감속이 제대로 걸리면 줄어야 한다.
- 킥이 걸렸다면 몇 번에 움직였는지(`out_pulse` 가 4로 튀는 구간).

---

## 2026-08-12 (5) — 종점 순차감속 재도입 ★이번엔 리니어로 줄인다★

### 무엇이 문제였나

(2) 에서 종점 선행제동을 걷어내면서 접근속도가 다시 **4펄스(3.54 m/s)** 가 됐고,
정지는 도착 판정 뒤의 리니어 2단에만 걸렸다. 그 조합의 숫자는 이렇다:

| 접근속도 | 5Hz GPS 샘플간격 | 리니어 정지거리(2.2 m/s²) |
|---|---|---|
| 3.54 m/s (4펄스) | **0.71 m** | **2.85 m** |
| 0.88 m/s (1펄스) | 0.18 m | 0.18 m |

반경 0.9m 창을 0.71m 간격으로 지나가니 **판정이 운에 달렸고**(통과 판정이 그래서
필요했다), 잡혀도 종점 2.85m 뒤에 선다 — `rec_20260811_214350` 의 오버런 2m 와
같은 자리다.

### 무엇을 바꿨나 — 세 단계

```
① 종점 5m   : 리니어 2단 + 목표펄스 0   (nxde 가 A보드 REF 를 0 으로 막는다)
② 4 km/h    : 리니어 해제 + 1펄스 크립  (저속 보정이 1펄스를 유지)
③ 도착      : DRIVE_DONE — 조향 0 + 리니어 2단, 완전정지 2초 뒤 해제 (종전 그대로)
```

검산 : 3.54 → 1.11 m/s 를 2.2 m/s² 로 줄이면 **1.1초·2.57m**. 5m 안에서 끝나고
남은 2.4m 를 크립으로 간다. 도착 순간 속도가 0.88 m/s 이므로 위 표의 아랫줄이 된다.

### 앞서 두 번 실패한 감속 정책과 무엇이 다른가

| | 버린 정책 | 버린 종점 선행제동 | **이번** |
|---|---|---|---|
| 수단 | 목표펄스를 0 으로 덮음 | 목표펄스를 미리 깎음 | **리니어를 문다** |
| 되먹임 | 실측펄스와 비교해 계속 물었다 폄 | 거리에 따라 매 틱 재계산 | **없음** — 거리로 한 번 물고 속도로 한 번 푼다 |
| 결과 | A보드 기동 블랭킹 재트리거(3회) | 정지를 대신 못 하고 구간만 길어짐 | 단조 전이 NONE→BRAKE→CREEP |

핵심은 **되돌아가지 않는다**는 것이다. 리니어 채터링도, 0↔양수 펄스 반복도 구조적
으로 생기지 않는다.

### 오작동을 막는 두 가지

- **진입에 `s_left`(남은 호길이)와 `d2goal`(직선거리)을 둘 다 요구한다.** 직선거리만
  보면 순환 코스에서 출발점이 종점 5m 안일 때 출발하자마자 제동이 걸리고, 호길이만
  보면 `wp_idx` 가 앞서 튀었을 때 걸린다. 둘 다여야 하므로 두 오작동이 동시에
  성립해야 한다. 늦게 걸리는 쪽으로 틀리면 종전 거동으로 돌아갈 뿐이다.
- **`GOAL_BRAKE_MAX_S`(3초) 탈출구.** `/speed` 도 엔코더도 없으면 속도 임계가 영영
  성립하지 않아 리니어를 문 채 굳는다 — 차는 서 있고 도착 판정도 서지 않는다.
  3초가 지나면 무조건 크립으로 넘어간다. 넘어가는 쪽이 언제나 안전하다.

### ⚠️ 임계가 IMU 속도라는 것

`speed.py` 헤더가 적어 둔 대로 **절대속도는 못 믿는다**(로그 대조 t=62 에서 GPS
17.4 vs IMU 8.1 km/h). 실제보다 일찍 '4km/h 아래'로 볼 수 있다. 그래도 위험이 작은
이유는 해제 뒤 명령이 1펄스뿐이고, 실측이 그보다 빠르면 저속 보정이 **0 으로 내려
코스팅**하기 때문이다(검증: 실측 2펄스·3.5펄스 → 출력 0, 실측 정지 → 출력 2).

### 검증

`goal_approach()` 를 스텁으로 떼어 5개 시나리오를 돌렸다 — 정상 접근(5m 제동 →
3.1km/h 해제 → 크립 유지), 속도 미수신 3초 탈출, 이미 느림(제동 생략), 순환 코스
출발점(미발동), `goal_brake_m=0`(종전 거동). **실차 검증은 아직이다.**

### 다음에 무엇을 확인해야 하나

- `goal_phase` 열(신설, `/drive_diag` 19번째)이 0→1→2 로 한 번씩만 올라가는지.
  1↔2 를 오가면 진입 조건이 흔들린 것이다.
- 해제 시점의 `speed_kmh` 와 그때 GPS 로 계산한 실제 속도의 차이. 해제가 이르면
  `goal_creep_kmh` 를 낮춘다(런치 인자).
- 최종 정지 위치가 종점에서 몇 m 인지. 설계상 0.2m 안쪽이어야 한다.
- 크립 구간에서 `out_pulse` 가 1~2 를 오가는지(정상) 0 으로 오래 머무는지(과속 판정).

---

## 2026-08-12 (4) — 기록 파일명에 경로 이름, GUI 메인화면(prompt_g), 런치 인자 누락 수정

세 가지를 함께 넣었다. 앞의 둘은 **로그를 나중에 읽는 사람**과 **차 옆에 서서 조작하는
사람**을 위한 것이고, 셋째는 (2) 에서 들어간 회귀다.

### 1. 주행 기록 CSV — 어느 경로로 달렸는지가 파일명에 없었다

#### 무엇이 관측됐나

8/11 하루에 `gps_data/` 에 경로 4개, `ros2bag/` 에 기록 7개가 쌓였다. 그런데 기록
파일명은 `rec_20260811_214350.csv` 처럼 **시각만** 담고 있어서, 파일 목록만 보고는
그 주행이 `route_20260811_160932` 을 따라간 것인지 `route_20260811_205252` 를
따라간 것인지 알 수 없다. CSV 를 열어 `/drive_cmd` 열을 ffill 해야 비로소 나온다 —
로그를 고르는 단계에서 매번 파일을 열어야 한다는 뜻이다.

#### 무엇을 바꿨나

```
종전 : ros2bag/rec_<날짜>_<시각>.csv
신   : ros2bag/<경로 CSV 이름>-<날짜>_<시각>.csv

  gps_data/route_20260811_160932.csv 로 주행
    → ros2bag/route_20260811_160932-20260812_134501.csv
```

뒤쪽 시각은 종전과 같은 **기록 시작 시각**이다. 같은 초에 두 번 시작하면 `_2` 가 붙는
규칙도 그대로다.

#### 경로 이름을 어떻게 아는가 — 두 곳에서 줍는다

| 출처 | 언제 오는가 | 역할 |
|---|---|---|
| `/drive_cmd` | prompt 가 경로를 고르는 순간 파일명 그대로 | **주 경로** |
| `/drive_event` | driving 의 `📁 경로 선택` · `▶ 주행 시작` | 보조 |

`record` 는 두 토픽을 **이미 구독하고 있었다**(표에 있는 열이다) — 새 구독을 늘리지
않고 지나가는 값을 붙들기만 한다. 보조 경로가 필요한 이유는 record 를 주행 도중에
새로 띄우면 `/drive_cmd` 를 이미 놓친 뒤이기 때문이고, 이때 `▶ 주행 시작(prompt)
[route_….csv]` 이벤트가 **`/drive_state` 보다 먼저** 나가므로(driving 의 `enter()`
안에서 `event()` 가 상태 발행보다 앞선다) 세션이 열리는 시점에는 이름이 도착해 있다.

- `STOP`·`MAP_START`·`DRIVE_START` 는 명령이지 파일명이 아니므로 거른다.
- 이벤트는 '경로 선택'·'주행 시작' 이 들어간 문장에서만 줍는다 — `❌ 경로 파일 없음:
  …/route_bad.csv` 같은 **실패 통보에서 이름을 주워 오지 않기 위해서**다.
- 끝내 못 들으면 `unknown-<시각>.csv` 로 적고 경고를 남긴다. **기록 자체는 거르지
  않는다** — 이름이 없다고 주행 데이터를 버리는 것이 훨씬 손해다.

기존 `rec_*.csv` 들은 건드리지 않는다.

### 2. `prompt_g.py` — prompt 와 같은 기능의 tkinter GUI

#### 왜

차 옆에서 조작할 때 CLI 는 키 입력과 화면 갱신이 섞여 읽기 어렵다. **기능은 그대로
두고 조작면만** 창으로 옮겼다. `prompt.py` 는 남는다(DISPLAY 없는 ssh 에서는 그쪽뿐).

```
 상태 IDLE  |  수동조종
 ┌ /speed  (IMU 환산)      ┐        [ 매 핑 ]
 ┌ /encoder (펄스 × 3.18)  ┐        [ 주 행 ]

 ┌ 조향각 명령 (−좌 / +우) ┐
 ┌ 주행펄스 명령           ┐        [ 종 료 ]
```

| 버튼 | 하는 일 |
|---|---|
| 매핑 | 수동조종이면 즉시 `MAP_START`, 자율주행이면 내릴 때까지 대기 후 발행. 매핑 중에는 **매핑 중단**(`STOP`) |
| 주행 | 파일 선택 창(`gps_data`)에서 경로를 고르면 이름을 발행 → 자율주행이면 즉시 `DRIVE_START`, 수동조종이면 올릴 때까지 대기. 주행 중에는 **주행 중단** |
| 종료 | **이 창(노드)만** 닫는다. 진행 중이면 정지 명령을 보낼지 먼저 묻는다 |

대기·시작 판정 규칙은 `prompt.py` 와 같다 — 화면이 `/vehicle_mode` 를 스스로 폴링해
목표 위치가 되는 순간 START 를 보내고, 안전 게이트는 `driving.cb_drive_cmd` 가 다시
본다. **둘을 동시에 띄우지 말 것**: 깨지지는 않지만(둘 다 발행자일 뿐) 두 화면이 서로
다른 대기 상태를 들고 있게 된다.

설계에서 지킨 것 두 가지:

- **값이 2초 넘게 안 들어오면 `—`.** 죽은 노드의 마지막 숫자를 현재값인 양 띄워 두는
  것이 제일 위험하다. IDLE 에서 `/cmd_vel_raw` 가 멎어 `—` 가 되는 것은 정상이다.
- **ROS 콜백은 값 대입만 하고 위젯을 만지지 않는다.** Tk 는 스레드 안전하지 않다 —
  화면 갱신은 메인 스레드의 `after()` 틱 한 곳뿐이다.

`gps_data` 밖의 파일을 고르면 막는다. `driving.select_route()` 는 `data_dir + 파일명`
으로 여니까 다른 폴더 파일은 이름만 넘어가 '경로 파일 없음' 이 된다.

> ⚠️ **`/encoder` 박스의 환산은 driving 과 다르다.** 지시대로 `펄스합 × 3.18` 만
> 했는데, `/encoder` 는 A보드 **좌+우 합**이라 `driving.py` 는 `ENC_SUM_TO_PULSE`
> (=0.5)를 먼저 곱해 바퀴 하나 기준으로 만든 뒤 3.182 를 곱한다. 즉 이 박스는
> driving 기준보다 **약 2배**로 읽힌다. 맞추려면 `prompt_g.py` 의
> `PULSE_TO_KMH` 를 3.18 → 1.59 로 바꾸면 된다. **미결 사항**이다.

### 3. `one_launch.py` — `cte_ki` 선언 누락 (★(2) 의 회귀★)

#### 무엇이 관측됐나

(2) 에서 `driving` 에 `cte_ki` 파라미터를 넘기도록 `parameters=` 에
`LaunchConfiguration('cte_ki')` 를 더했는데, `DeclareLaunchArgument` 를 빼먹었다.
선언 없는 launch configuration 은 **치환 시점에** 터진다:

```
[ERROR] [launch]: Caught exception in launch: launch configuration 'cte_ki' does not exist
```

`ros2 launch white1 one_launch.py` 가 **통째로 뜨지 않는다.** 문법 오류가 아니라
실행 시점 오류라 파일을 읽는 것만으로는 드러나지 않고, `--show-args` 로도 안 잡힌다.

#### 고친 것

`cte_ki` 를 `driving.py` 의 `CTE_KI` 와 같은 기본값 **0.30** 으로 선언에 추가했다.

> **규칙**: `parameters=` 에 `LaunchConfiguration('x')` 를 더할 때는 `args` 리스트의
> `DeclareLaunchArgument('x', …)` 를 **같은 커밋에서** 반드시 함께 넣는다.

### 다음에 무엇을 확인해야 하나

- 다음 주행 뒤 `ros2bag/` 에 `route_…-….csv` 이름으로 파일이 생겼는지. `unknown-` 이
  나오면 record 가 `/drive_cmd`·`/drive_event` 를 모두 놓친 것이므로 기동 순서를 본다.
- `prompt_g` 로 매핑 1회·주행 1회. 특히 **대기 → 스위치 전환 → 자동 시작** 경로와
  **중단 버튼**을 실차에서 한 번씩.
- `/encoder` 박스를 driving 기준(×1.59)으로 맞출지 결정.

---

## 2026-08-12 (3) — 가변저항 하드리밋 재측정에 따른 조향 전달계 재환산

### 무엇이 바뀌었나

B보드 DC모터 가변저항 실측값 (왼쪽 신 / 오른쪽 구):

| | 신 | 구 |
|---|---|---|
| L | 618 | 576 |
| C | 473 | 469 |
| R | 328 | 362 |

두 조 모두 **C 가 정확히 L·R 의 중점**이다. `kasa_0804_B.ino` 가
`SAFETY_MARGIN`(10)을 뺀 구간을 −40~+40° 에 선형으로 걸기 때문이다:

```
구 : 566 … 372 = 194 카운트 / 80° = 2.425 카운트/도   (중심 469)
신 : 608 … 338 = 270 카운트 / 80° = 3.375 카운트/도   (중심 473)
```

**같은 물리 위치를 만드는 지령 각도가 2.425/3.375 = 0.7185배로 작아졌다.**
하드리밋이 넓어진 만큼 1도당 실제 행정이 커졌기 때문이다.

### 고친 값

`STEER_PLANT_GAIN` 과 `STEER_UNDERSTEER` 은 둘 다 '지령 각도 → 실제 거동'의
비율이므로 같은 계수로 함께 줄인다.

| 상수 | 구 | 신 |
|---|---|---|
| `STEER_PLANT_GAIN` | 1.75 | **1.26** |
| `STEER_UNDERSTEER` | 7.2 | **5.17** |
| 도로휠각 상한 (=40/gain) | 22.8° | **31.7°** |
| 최소회전반경 | 2.97 m | **2.02 m** |
| 포화 없는 LFD 문턱 (=2·R) | 2.98 m | **4.04 m** |

launch 기본값도 같이 바꿨다(`steer_plant_gain`, `steer_understeer`).

### 검산 — 같은 물리 위치를 내는가

같은 도로휠각 요구에 대해 두 설정이 만드는 실제 raw 위치:

| 도로휠 | v | 구 pot → raw | 신 pot → raw | 차 |
|---|---|---|---|---|
| 2° | 1.77 | 4.13° → 459.0 | 2.97° → 463.0 | +4.0 |
| 5° | 3.54 | 15.07° → 432.5 | 10.83° → 436.4 | +4.0 |
| 10° | 3.54 | 30.23° → 395.7 | 21.74° → 399.6 | +3.9 |
| 14° | 3.54 | **40.00° → 372.0 (포화)** | 30.56° → 369.9 | −2.1 |
| 20° | 3.54 | **40.00° → 372.0 (포화)** | **40.00° → 338.0** | −34.0 |

**차이가 정확히 +4 카운트로 일정하다 = 기울기는 맞았고 중심 오프셋만 남는다.**
그리고 도로휠 ~13° 위에서는 **구 설정이 pot 40° 에 포화해 raw 372 에서 더 못 갔다**
— 하드리밋(338)까지 34 카운트, 편측 행정의 12% 를 못 쓰고 있었던 것이다.

### ★실질적 효과 — 이 경로의 코너를 이제 포화 없이 돈다★

`route_20260811_205252` 의 최대 요구 도로휠각 17.0° 에서:

| 속도 | 구(1.75/7.2) | 신(1.26/5.17) |
|---|---|---|
| 1펄스 0.88 m/s | 31.1° | 22.4° |
| 2펄스 1.77 m/s | 35.3° | 25.4° |
| 3펄스 2.65 m/s | **42.1° 포화** | 30.3° |
| 4펄스 3.54 m/s | **51.8° 포화** | 37.3° |

구 설정은 **3펄스 이상이면 이 코너에서 조향이 포화**했다. 어제 로그에서 코너를
1~2펄스로 기어갈 수밖에 없었던 것과 앞뒤가 맞는다. 신 설정은 4펄스에서도
37.3° 로 포화 직전에 걸린다.

### 바뀌지 **않는** 것 — 조향 불감대

`STEER_TOLERANCE_EXIT` = 6 raw 카운트는 물리량이라 재측정으로 바뀌지 않는다.
pot 각도로 부른 값만 달라진다:

```
구 매핑 → 2.47° pot  ┐ 둘 다 도로휠 기준 1.41°
신 매핑 → 1.78° pot  ┘ (카운트/도로휠도 = 4.25 로 동일)
```

조향이 반응하지 못하는 측방오차도 그대로다: LFD 5.16 m → **0.26 m**.

> ⚠️ **종전 주석의 0.46 m 는 틀린 값이었다.** pot 각도를 그대로 기하식에 넣어
> 링키지비(1.75)만큼 부풀린 결과다. 맞는 값은 0.26 m 이고, 이건 재측정과 무관하게
> 종전에도 0.26 m 였다. **CTE 적분항의 근거는 그대로 유효하다** — 관측된 편향
> +0.13~0.22 m 는 여전히 이 문턱 아래라 순수추종이 반응하지 못한다.

### 남은 것

- **중심 오프셋 +4 카운트**(= pot 1.19°, 도로휠 0.94°). 구 매핑의 0° 와 신 매핑의
  0° 가 그만큼 다르다. 어느 쪽이 진짜 직진인지는 이 값들만으로 알 수 없다(두 C 값
  모두 하드리밋의 산술중점일 뿐 직진 실측이 아니다). **크기가 CTE 적분항의 작동
  범위 안이라 적분이 흡수한다** — 다음 주행에서 `cte_i_term_deg` 가 이 근처
  (0.9° 도로휠)에서 안정되는지 보면 오프셋의 실체를 역으로 알 수 있다.
- ⚠️ **원 실측(126표본)은 구 매핑으로 달린 로스백이다.** 실측을 다시 한 것이 아니라
  환산만 했으므로, 다음 주행에서 코너 추종을 반드시 확인할 것. 여전히 크게 돌면
  `steer_plant_gain` 을 올린다(= 더 꺾는다).

---

## 2026-08-12 (2) — CTE 적분항(Ki 단독) 이식

구 `white` 패키지와 주행 단계를 대조한 결과 중 **하나만** 되살렸다.

### 왜 이것부터인가 — 로그가 '진동'이 아니라 '편향'이라고 말한다

| | 214350 | 214852 |
|---|---|---|
| 평균 CTE | **+0.222 m** | **+0.135 m** |
| 부호 + 비율 | **76 %** | **84 %** |
| 부호반전 | 3.1 회/분 | 3.0 회/분 |

반전이 분당 3회뿐이니 사행·발산이 아니다. 부호가 8할 가까이 한쪽(+ = 경로 왼쪽)
으로 몰린 **정상상태 오차**이고, 그것이 정확히 적분이 지우라고 있는 오차다.
구 white 의 게인표 주석도 같은 결론이었다 — "진동은 거의 없고 한쪽 쏠림이
문제였으므로 P 는 보수적으로 두고 Ki 로 정상상태 오차를 천천히 제거한다".

**순수추종은 이 오차를 원리적으로 못 지운다.** B보드 조향 불감대
(`STEER_TOLERANCE_EXIT` 6카운트 = 2.47° pot)는 LFD 5.16 m 에서 측방오차 **0.46 m**
에 해당한다. 관측된 +0.13~0.27 m 편향은 그 아래라 조향이 물리적으로 반응하지
않는다. 시간을 들여 그 문턱을 넘을 수 있는 것은 적분뿐이다.

### 무엇을 넣었나

`pure_pursuit_steer()` 가 낸 **도로휠각**에 적분항만 더한다(P·D 없음).
P 를 넣으면 순수추종 기하와 권한이 겹쳐 두 배로 꺾이므로 지금은 넣지 않는다.

```
road = pure_pursuit(lfd) + clamp(CTE_KI · ∫cte dt,  ±CTE_I_MAX_DEG)
```

| 상수 | 값 | 뜻 |
|---|---|---|
| `CTE_KI` | 0.30 | deg(도로휠)/(m·s). launch 인자 `cte_ki`, **0 이면 끈다** |
| `CTE_I_CLAMP` | 8.0 | 적분값 클램프 [m·s] |
| `CTE_I_MAX_DEG` | 2.5 | 기여 상한 [deg 도로휠] = pot 4.38° |
| `CTE_I_DEADBAND_M` | 0.05 | 이 안이면 적분 않고 감쇠 |
| `CTE_I_FLIP_SCALE` | 0.35 | 부호반전 시 소프트 감쇠 |
| `CTE_I_MIN_PULSE` | 0.3 | 실측이 이 밑이면 **동결** |

**부호**: CTE + = 차가 경로 왼쪽 → 우조향(+) 필요 → 그대로 더한다(뒤집지 않는다).
**단위**: 도로휠각에 더하므로 `steer_command()` 의 링키지비 1.75 를 타고 pot 으로
나간다 — pot 기준으로는 위 값의 1.75배다.

### 와인드업 방어 — 구 white 의 순서를 그대로, 여기에 하나 추가

1. 부호반전 → ×0.35 **소프트 감쇠**(완전 리셋 아님). 완전히 지우면 좌우 왕복마다
   리셋돼 실제 쏠림을 못 쌓는다 — 구 white v6.6 의 결론을 그대로 가져왔다.
2. 불감대 안이면 적분하지 않고 감쇠(0.5/s), 밖이면 적분
3. 적분값 클램프 `CTE_I_CLAMP`
4. 기여 자체도 클램프 `CTE_I_MAX_DEG`
5. **★차가 안 구르면 동결★** ← 이 차 고유. 어제 코너에서 10초를 멈춘 채 CTE 가
   +0.17 m 로 굳어 있었는데, 안 구르는 차에 조향 적분을 쌓으면 출발 순간 그만큼
   튄다. 그 구간은 쌓지도 줄이지도 않는다.
6. `enter()` 가 상태 전이마다 적분을 리셋한다(이전 구간 누적이 다음 구간 첫 틱에
   실리는 것을 막는다).

### 실측 궤적 리플레이 (rec_20260811_214852 의 CTE 를 그대로 흘림)

| t | CTE | 적분[m·s] | i_term(도로휠) | → pot |
|---|---|---|---|---|
| 14 | −0.026 | −1.19 | −0.36° | −0.62° |
| 29 | +0.170 | 0.82 | +0.25° | +0.43° ← **동결**(정지구간) |
| 39 | +0.190 | 1.35 | +0.41° | +0.71° |
| 49 | +0.340 | 3.66 | +1.10° | +1.92° |
| 54 | +0.438 | 5.60 | +1.68° | **+2.94°** ← 불감대(2.47°) 돌파 |
| 59 | +0.344 | 8.00 | +2.40° | +4.20° ← 클램프 |

정지구간 동결이 의도대로 걸리고, 편향이 누적돼 t≈52 에 조향 불감대를 넘는다.
※ 이건 **개루프 리플레이**다 — 실제로는 조향이 먹히는 순간 CTE 가 줄어 적분도
멈추므로 클램프까지 가지 않는 것이 정상이다. 클램프에 붙어 산다면 게인이
부족하거나(더 빨리 지워야 함) 편향의 원인이 조향이 아닌 다른 데 있다는 뜻이다.

검증용으로 `/drive_diag` 에 `cte_integral` · `cte_i_term_deg` 2열을 추가했다
(record 16→18열).

### 되살리지 않은 것 (구 white 대비, 주행 단계)

P·D 항, 게인 스케줄 표(`GAIN_TABLE` 12행), 지연보상(상태예측 τ=0.5 s + 앞차축
lead), 조향 슬루·평활·잔떨림 억제·저속 조향권한 램프, 속도 폐루프 PI,
가감속률 제한, 출발 램프, CTE_RECOVERY·STEER·APPROACH·CUSP·DR·TL_HOLD 속도 캡,
후진/cusp, 카메라 융합, 지형·피치, gps_imu 의 뒷차축 투영·DR 폴백·drift 보정.

### 다음 주행에서 확인할 것

1. `cte_i_term_deg` 가 자라는가, 그리고 자란 뒤 `cte_m` 의 평균이 0 쪽으로
   내려오는가. **평균 CTE 가 목표 지표**다(어제 +0.22/+0.14).
2. 부호반전 횟수가 늘지 않는가(분당 3회 기준). 늘면 적분이 진동을 만든 것이니
   `cte_ki` 를 낮추거나 0 으로 끈다.
3. `cte_integral` 이 클램프(±8.0)에 붙어 사는가 — 붙어 있으면 위 ※ 참고.
4. 정지구간에서 `cte_integral` 이 멈춰 있는가(동결 동작 확인).

---

## 2026-08-12 — IMU 속도계 신설 / 저속 펄스 보정 / 제동 중 구동 차단 / 순차 감속 제거

### A. `speed.py` 신설 — IMU 적분 속도계 `/speed` [km/h]

엔코더도 GPS 도 저속에서 못 쓴다는 것이 어제 로그로 드러났다(엔코더는 정지 중
2.5~5펄스의 허수 카운트, GPS 는 5Hz 라 최소 0.2초 지연 + 저속에서 노이즈가 지배).
그래서 20Hz IMU 를 적분하는 노드를 만들었다.

**설계 — 실측으로 정한 것**

1. **축 하나를 그냥 적분하면 안 된다.** 정지 시 가속도 평균이
   `(−5.21, −1.05, +8.25) m/s²`, 크기 정확히 9.81 — 가속도계는 중력을 포함하고
   센서가 32° 기울어 장착돼 있다. 축별 단독 적분은 26초에 `x −1.86 / y +1.83 /
   z −1.10 m/s` 로 **부호조차 안 맞는다**(실제 +3.5).
2. **쿼터니언으로 월드 변환하면 맞는다.** 정지 시 월드 가속도가
   `(0.068, 0.027, 9.810)` 으로 중력이 Z 에 정확히 정렬된다.
3. **ZUPT(영속도 보정)로 드리프트를 잡는다.** 순수 IMU 는 '정지'와 '등속'을
   원리적으로 구별 못 하지만, 이 차에서는 노면 진동이 구별해 준다 —
   `|a|` 의 0.5초 이동 표준편차가 완전정지 0.064 / 0.05~0.3 m/s 0.279 /
   주행 0.58~1.14 로 **15배 벌어진다**. 임계 0.15 면 기어가는 것을 정지로
   오판하지 않는다.

**★한계 — 반드시 알고 쓸 것★**

로그를 이 알고리즘에 그대로 흘려 GPS 와 대조했다:

| 시각 | speed.py | GPS 실측 |
|---|---|---|
| t=40 | 0.75 | 1.18 km/h |
| t=50 | 1.68 | 4.16 |
| t=56 | 3.11 | 8.71 |
| t=62 | 8.13 | **17.37** |

절반 수준으로 과소평가한다. 원인은 적분이 아니라 **AHRS 자세가 지속 가속에
오염되는 것** — 피치가 정지 32.49° → 주행 30.10° 로 2.4° 흔들렸고, 중력 누출
0.41 m/s² 는 이 주행의 평균 가속도 0.185 m/s² 보다 크다. 자세가 우리가 재려는
신호를 스스로 지운다.

자세를 자이로로만 전파해도(스트랩다운) 나아지지 않는다 — 이번엔 반대로
**1.6배 과대평가**(t=62 에서 26.83 / 17.37). 자이로 드리프트가 반대 방향으로
중력을 새게 한다.

**결론**: 이 하드웨어에서 순수 IMU 적분의 절대속도는 26초 창에서 2배 수준으로
틀린다. 대신 **'서 있는가 움직이는가'는 아주 정확하다** — 그리고 그것이 엔코더가
못 하는 일이다. 아래 B 의 보정량을 ±2펄스로 묶고 REF 3 이하로 제한한 이유가 이것.

---

### B. 저속 펄스 보정 — `driving.py`

지령 1펄스를 30.8초 줬는데 10초를 아예 서 있었던 구간(어제 E 항)에 대한 대응.
저속에서는 인휠 토크와 타이어 스크럽 부하가 맞물려 지령과 실제 속도의 관계가
무너지므로, **낮은 REF 에서만** 실제 속도를 보고 REF 를 밀어 준다.

```
out = REF + clamp(REF − 실측펄스, −2, +2),   0 ≤ out ≤ 15
```

동작 조건을 좁게 잡았다:

- **`DRIVE_RUN` 이고 `1 ≤ REF ≤ 3` 일 때만.**
  REF 0 에서는 절대 걸지 않는다(세우려는 지시를 보정이 뒤집으면 안 된다).
  REF 4 이상에서 걸면 `MAX_PULSE_LIMIT`(4)를 우회하는 과속 경로가 된다.
  `DRIVE_HEADING` 은 곧게 굴러 헤딩을 잡는 구간이라 속도를 흔들면 안 된다.
- **실측은 `/speed` 우선, 없거나 0.5초 넘게 오래됐으면 엔코더로 내려간다.**
- **0.3초 주기로만 다시 판단한다**(REF 가 바뀌면 즉시). 20Hz 로 매 틱 계산하면
  속도가 따라오기 전에 보정이 널뛴다.

동작 확인:

| REF | 실측 | 발행 |
|---|---|---|
| 3 | 1펄스 | **5펄스** ← 지시하신 예시 |
| 3 | 2펄스 | 4펄스 |
| 3 | 3펄스 | **3펄스** ← 속도 도달, 보정 해제 |
| 3 | 5.5펄스 | 1펄스 |
| 4 | 1펄스 | 4펄스 (미동작) |
| 0 | 0펄스 | 0펄스 (미동작) |

`MAX_PULSE_LIMIT`(4)와의 관계: 그 상한은 **기준속도(REF)의 천장**이고 보정은
**기동을 위한 일시적 가산**이라 성격이 다르다. 그래서 자르는 순서가 REF → 보정
→ 0~15 이고, 실제 상한은 3+2 = **5펄스**다.

검증 가능하도록 `/drive_diag` 에 `ref_pulse` · `out_pulse` · `meas_pulse` 3열을
추가했다(record 도 함께 갱신). **`out ≠ ref` 인 구간이 보정이 걸린 구간**이다.

---

### C. 제동 중 구동 차단 — `nxde/arduino.py`

(4) 정상 자율주행 분기에서 **브레이크가 0 이 아니면 A보드 REF 를 무조건 0** 으로
보낸다. 구동과 제동을 동시에 걸면 리니어는 차를 잡으려 하고 인휠은 목표펄스를
맞추려 전류를 더 밀어넣어 서로 밀어낸다 — 차가 안 서고, 모터·리니어가 서로
부하가 되며, A보드가 '지령대로 안 구른다'고 보고 기동 블랭킹을 재트리거해
허수 카운트까지 쏟아진다.

driving 은 `DRIVE_DONE` 에서 이미 펄스 0 을 보내므로 평소에는 값이 같다.
이 줄이 실제로 일하는 경우는 **주행 중에 브레이크를 요청하는 다른 발행자**
(예: `camera_judgment`)가 있을 때다.

---

### D. 순차 감속 제거 — 어제 넣은 종점 선행제동을 걷어냈다

어제 넣은 `GOAL_BRAKE_DECEL`/`GOAL_BRAKE_MARGIN_M`(종점 4→3→2펄스 순차 감속)을
**제거**했다. 정지는 리니어 2단이 담당하고(실측 감속도 2.2 m/s²), 속도를 미리
깎는 것은 그 일을 대신하지 못하면서 종점 구간만 길게 만든다.

**지금 정책**: 종점까지 곡률이 허락하는 속도로 가고, 도착 또는 코스 이탈 판정이
서는 순간 `DRIVE_DONE` 으로 넘어가 **조향 0(일직선) + 리니어 2단**으로 세운다.
완전정지(엔코더 0)가 2초 유지되면 리니어 0단 + IDLE.

⚠️ **그래서 도착 판정이 유일한 방벽이 됐다.** 접근속도가 다시 4펄스대로 올라가므로
반경(0.9 m)만으로는 5Hz GPS 에서 놓칠 수 있고, **통과 판정**(마지막 WP 가 차체 뒤로
넘어감)이 반드시 함께 있어야 한다 — 어제 넣은 그 조건이 이제 주 판정이다.
리니어 체결 시점 속도가 3.5~4.8 m/s 이므로 **정지 위치는 종점에서 약 2 m 뒤**가
된다(어제 실측 정지거리).

### 변경 파일

| 파일 | 내용 |
|---|---|
| `white1/speed.py` | **신설** — /imu 적분 → /speed [km/h], ZUPT 포함 |
| `white1/driving.py` | 종점 선행제동 제거 / `/speed` 구독 / `measured_pulse()`·`low_speed_trim()` 신설 / `send()` 가 REF→보정→발행 / 진단 3열 추가 |
| `white1/record.py` | `/speed` 열 추가, `/drive_diag` 13→16열 |
| `nxde/nxde/arduino.py` | (4) 분기에서 브레이크>0 이면 A보드 REF 0 |
| `white1/setup.py` | `speed` 실행파일 등록 |
| `launch/one_launch.py` | speed 노드 추가 |

### 다음 주행에서 확인할 것

1. **노드 재시작**(어제 것도 아직 안 실렸다). `speed` 는 새 실행파일이라
   `colcon build` 가 한 번 필요하다 — `--symlink-install` 이라도 entry point 등록은
   빌드가 해야 한다.
2. `/speed` 가 정지에서 정확히 0 인가, 기어갈 때 0 을 벗어나는가.
3. `out_pulse ≠ ref_pulse` 구간에서 차가 실제로 굴렀는가(`ego_x_m`/`ego_y_m` 변위로).
4. 보정이 채터링하지 않는가(`out_pulse` 가 3↔5 로 널뛰지 않는지).
5. `🎯 도착` 또는 `🎯 도착(통과)` 이벤트가 나오는가 — 순차 감속을 뺐으므로
   **통과 판정이 주 방벽**이다.
6. 종점 정지 위치가 종점에서 몇 m 뒤인가.

---

## 2026-08-11 — 종점 질주 / 도착 판정 실패 / 코너 저속

### 근거 로그

| | `rec_20260811_214350` | `rec_20260811_214852` |
|---|---|---|
| 경로 | `route_20260811_205252` (161 WP, 54.1 m) | 동일 |
| 종료 사유 | `🚨 경로이탈 CTE −2.38 m` (t=63.26) | `🚨 경로이탈 CTE −2.09 m` (t=63.11) |
| 최종 정지 위치 | 종점 **4.40 m 지나서** | 종점 **2.20 m 지나서** |
| `🎯 도착` 이벤트 | **없음** | **없음** |

두 주행 모두 도착으로 끝나지 못했다. 차를 세운 것은 도착 판정이 아니라
`CTE_DEVIATION_M`(2.0 m) 이탈 감지였다.

---

### A. 종점을 4펄스로 질주했다

**관측** (GPS 변위 기준 실제 속도, 214852)

| 종점까지 남은 거리 | 실제 속도 |
|---|---|
| 3.25 m | 4.58 m/s |
| 1.14 m | **4.83 m/s = 17.4 km/h** |
| 0.40 m | 3.74 m/s |

**원인 1 — 종점 감속 항이 코드에 아예 없었다.**
`corner_speed()` 는 곡률만 보고 속도를 정했고 `d2goal` 인자조차 없었다.
`lookahead_m()` 의 종점 캡은 **LFD 만** 줄이고 속도는 건드리지 않는다.
경로 곡률을 오프라인 재계산해 보면 마지막 8 m 는 전부 4펄스 지령 구간이었고,
실제 로그에서도 t=60.1(종점 7.13 m 전)에 4펄스로 올라가 그대로 종점에 꽂았다.

**원인 2 — 지령보다 실제가 더 빠르다.**
4펄스 지령(3.54 m/s)인데 GPS 실측 4.83 m/s. 개루프 펄스 지령이라 실제 속도가
지령을 1.36배 넘겼다.

**수정**

- `driving.py` 에 **종점 선행제동** 추가 — 코너 게이팅과 같은 식, 감속도만 완만하게.
  ```
  v_goal = √(2 · GOAL_BRAKE_DECEL · (남은거리 − GOAL_BRAKE_MARGIN_M))
  GOAL_BRAKE_DECEL    = 1.5 m/s²
  GOAL_BRAKE_MARGIN_M = 1.0 m
  ```
  `corner_speed()` 가 `d2goal` 을 인자로 받고 (c) 항에서 이 값으로 목표속도를 누른다.
  하한은 기존 `CORNER_MIN_PULSE`(2) 를 그대로 쓴다.

  실제 경로에 적용한 결과:

  | 종점까지 | 지령 |
  |---|---|
  | 4.3 m | 4펄스 |
  | 4.0 m | 3펄스 |
  | 2.4 m | 2펄스 |
  | 도착까지 | 2펄스 유지 (1.77 m/s) |

- **최고속도 절대 상한** `MAX_PULSE_LIMIT = 4` 신설.
  `send()` 가 마지막에 무조건 이 값으로 자르고, 파라미터(`drive_pulse`,
  `heading_pulse`)도 읽는 즉시 같은 값으로 자른다.
  ※ `send()` 에서만 자르면 `max_speed_ms`(속도대역·LFD 식의 기준)가 안 잘린 값으로
  남아 계산과 출력이 어긋나므로 두 곳 다 잘라야 한다.
  기존 상한은 프로토콜 상한 15 라 사실상 없는 것과 같았다.

---

### B. 도착 판정이 원리적으로 성립할 수 없었다

**관측** — 최근접 거리 0.24 m(214350) / 0.38 m(214852). 둘 다 `WP_REACH_M`(0.2 m) 밖.

**원인** — GPS 는 5 Hz(0.20 s)다. 종점 접근속도 4.8 m/s 면 샘플 간 이동거리가
**0.96 m** 라 반경 0.2 m 창을 통째로 건너뛴다. 조건이 반경 하나뿐이었으므로
(`wp_idx == 마지막 and d2goal <= 0.2`) 한 번 놓치면 그대로 질주했다.

**수정** — `run_follow()` 의 종점 판정을 둘로 늘렸다. **둘 중 먼저 되는 쪽이 도착**이다.

1. **반경** : `WP_REACH_M` 0.2 → **0.9 m** (launch `wp_reach_m` 기본값도 함께)
   A 의 종점 감속으로 접근속도가 1.77 m/s 가 되어 샘플 간격이 0.35 m 이므로
   0.9 m 창은 2~3회 샘플된다.
2. **통과** : 마지막 WP 가 차체기준 뒤로 넘어갔으면(전방거리 < 0) 도착.
   반경은 '가까웠던 순간을 샘플했는가'에 의존하지만 통과는 한 번 지나가면 반드시
   성립한다 — 종점 질주의 마지막 방벽이다. 이격 `GOAL_PASS_MAX_M`(2.5 m) 이내에서만
   인정하고, 그보다 멀면 '지나쳤다'가 아니라 '벗어났다'로 보고 CTE 이탈감지에 맡긴다.

두 판정 모두 **남은 호길이 `GOAL_ZONE_M`(6.0 m) 이내에서만** 본다 —
순환 코스에서 출발점 부근을 종점으로 오검출하는 것을 막는다.

---

### C. 종점에서 조향이 폭주했다

**관측** (214350)

```
종점 1.36 m : 조향지령 +25.2°
종점 0.64 m : 조향지령 +40.0°   ← pot 상한 포화
그 다음 틱  : 조향지령   0.0°   ← lx<=0.05 조기탈출
실측 조향각 : 지령이 0 으로 떨어진 뒤에도 계속 감 → +34° 까지
```

**원인** — 목표점이 마지막 WP 로 붕괴하면 순수추종 분모가 `lfd·0.5`(≈1.15 m) 바닥에
걸리고, 작은 측방 오차에도 alpha 가 커져 조향이 포화한다. 그리고 지령을 한 틱에
40°→0° 로 끊어도 B보드는 못 따라온다(실측 슬루 70°/s, 불감 0.25 s).

**수정** — B 의 도착 판정이 이 구간에 들어가기 전에 `DRIVE_DONE` 으로 넘기므로 폭주
구간 자체가 사라진다. 더해서 `enter(S_DRIVE_DONE)` 이 **조향 0(일직선) · 펄스 0 을
리니어 2단과 같은 틱에** 내보내도록 바꿨다. 예전에는 다음 `loop()` 틱(최대 50 ms 뒤)
에야 조향 0 이 나갔고, 그 50 ms 동안 B보드가 직전 각을 향해 계속 슬루했다.

---

### D. 리니어는 정상이었다 — 늦게 물렸을 뿐이다

관찰상 "도착했는데 리니어가 안 걸린다"였지만 로그는 다르게 말한다.

- `/brake_level = 2` 는 정상 발행됐고, `nxde/arduino.py` 의 **(4) 정상 자율주행**
  분기가 그대로 B보드로 넘긴다. 경로에 막히는 곳이 없다.
- 214350 제동 후 감속 : 2.09 → 1.30 → 0.55 → 0.08 m/s (0.6 초), 총 정지거리 약 2 m,
  평균 **2.2 m/s²**. 물리적으로 작동했다.

문제는 **언제** 물렸는가다. 도착 판정(B)이 실패했으므로 리니어를 부른 것은 도착이
아니라 CTE 이탈 감지였고, 그 지점이 이미 종점 2 m 뒤였다. 그래서 밖에서는 안 걸린
것처럼 보였다. **고친 곳은 리니어가 아니라 A·B 다.**

또 하나, 그때의 CTE 는 옆으로 벗어난 거리가 아니다 — 마지막 WP 를 지나면
`signed_cte()` 가 마지막 선분 끝점으로 clamp 되므로 `cte == goal_dist` 가 된다
(로그에서 두 열이 정확히 일치한다). 이탈 감지기가 우연히 '종점 2 m 초과 통과
감지기' 역할을 하고 있었던 셈이다.

**도착 시 동작(확정)**

```
종점 도달 → 조향 0(일직선) + 펄스 0 + 리니어 2단   (같은 틱)
         → 엔코더 0 이 2초 유지되면 리니어 0단 + IDLE(메뉴 복귀)
```

하한이 2펄스이므로 도착 순간 속도는 1.77 m/s 이고, 리니어 2단이 나머지를 맡는다
(관측 감속도 2.2 m/s² → 정지거리 약 0.7 m). **종점을 0.7 m 정도 지나 서는 것은
설계된 동작이다.**

---

### E. 코너에서 차가 거의 안 굴렀다 — 코드는 이미 고쳐져 있었고, 안 실린 것이다

**관측** (214852)

| 구간 | 지령 | 실제 속도 |
|---|---|---|
| t=26~36 (10 초) | 1펄스 | **0.00 m/s — 완전 정지** |
| t=36~52 (16 초) | 1펄스 | 0.04 → 1.00 m/s |

`cmd_pulse = 1` 이 주행의 **52 %(30.8 초)** 를 차지했고, 그 동안 `wp_idx` 는
65 → 89, 즉 31 초에 6 m 갔다.

**원인 1 — 정수 펄스 반올림이 하한을 30 % 깎는다.**
`min_speed_ms = max(0.9, 3.54/2.8) = 1.26 m/s` → `int(1.26/0.884+0.5) = 1펄스
= 0.884 m/s`.

**원인 2 — 감속 곡선이 이 경로에 대해 너무 가파르다.**
이 경로의 요구 도로휠각은 win 최대 15.4° / peak 최대 17.0° 인데
`STEER_FULL_SLOWDOWN_DEG = 15.0` 이다. 코너 구간(WP 65~90, 약 12 m) 전체가
"최대 감속" 영역에 그대로 걸린다 — 곡률 비례 감속이 아니라 사실상
**코너 진입 = 최저속도 고정**이다.

**원인 3 — `CORNER_MIN_PULSE = 2` 가 이 두 주행에 실리지 않았다.**
현 소스는 하한을 2 로 묶으므로 1펄스가 나올 수 없다. 그런데 로그에는 1펄스가
51~52 % 찍혀 있다 → **노드를 재시작하지 않아 반영되지 않은 채 달렸다**(확인됨).

**수정** — 코드 변경 없음. 하한 2 는 이미 들어가 있다. 상수 주석에
**"아직 실차 검증 전"** 을 명시했다. 원인 2(`STEER_FULL_SLOWDOWN_DEG`)는
하한 2 를 실제로 태워 본 뒤에 판단한다 — 먼저 건드리면 무엇이 효과를 냈는지
구분할 수 없다.

---

### F. 고치지 않고 기록만 남긴 것

- **엔코더가 저속에서 못 믿을 값을 뱉는다.** t=32 에 차는 GPS 상 완전히 서 있는데
  `encoder_sum` 이 2.5~5펄스를 뱉었다(A보드 기동 블랭킹 허수 카운트). 중앙값 3점
  필터로는 안 죽는다.
- **그 허수가 리니어 해제를 늦춘다.** 214350 에서 차는 t=64.46 에 이미 정지했는데
  엔코더가 t=65.46 에 한 번 1.0 펄스로 튀어 2초 타이머가 리셋됐고, 해제가 2.0 초가
  아니라 4.35 초 뒤에 났다. **늦게 푸는 쪽은 안전한 방향**이라 그대로 뒀다 —
  임계를 올리면 '아직 구르는데 푼다'는 반대쪽 위험이 생긴다.

---

### 변경 파일

| 파일 | 내용 |
|---|---|
| `white1/driving.py` | `MAX_PULSE_LIMIT` 신설 / `GOAL_BRAKE_*` 신설 / `WP_REACH_M` 0.2→0.9 / `GOAL_PASS_MAX_M`·`GOAL_ZONE_M` 신설 / `corner_speed()` 에 종점 선행제동 (c) 추가 / `run_follow()` 종점 판정 = 반경 or 통과 / `enter(DRIVE_DONE)` 이 조향 0·펄스 0 을 리니어와 같은 틱에 발행 / 파라미터 상한 클램프 |
| `launch/one_launch.py` | `wp_reach_m` 기본값 0.2→0.9, `drive_pulse` 설명에 상한 명시 |

### 다음 주행에서 확인할 것

1. **노드를 반드시 재시작한다** — 이번 수정도, 이전 `CORNER_MIN_PULSE=2` 도 재시작
   없이는 실리지 않는다. (`--symlink-install` 이라 재빌드는 불필요하고 재시작만
   하면 된다.)
2. `🎯 도착` 이벤트가 나오는가. 반경으로 났는가 통과로 났는가 — 이벤트 문구로 구분된다.
3. 종점 접근 속도가 2펄스(1.77 m/s)로 떨어지는가 (`cmd_pulse`, `goal_dist_m` 열).
4. 종점 부근 `cmd_steer_deg` 가 포화(±40°)하지 않는가.
5. 코너 구간에서 `cmd_pulse` 가 2 로 유지되고 차가 실제로 구르는가
   (`encoder_sum` 이 아니라 **`ego_x_m`/`ego_y_m` 변위로 판정할 것** — F 참고).
6. 정지 후 리니어 해제까지 걸린 시간.
